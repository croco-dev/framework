---
"@croco/storage-cloudinary": patch
---

Generate signed Cloudinary downloads through the authenticated download API so the returned URL is rejected after its requested expiry while public delivery URLs remain unchanged.
