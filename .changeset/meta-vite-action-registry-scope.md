---
"@croco/meta-vite": patch
---

Server actions can now use scoped registries with reset and unregister cleanup paths for tests, HMR, and multi-app runtimes while preserving the global registry API.
