---
"@croco/cli": patch
---

`croco doctor` can now diagnose workspace package discovery, repository boundary violations, and Lambda telemetry flush gaps with stable recovery-focused diagnostic codes.
