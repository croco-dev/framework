---
"@croco/transports-http": patch
---

Document and verify the HTTP HEAD/GET interaction policy so GET-only routes provide bodyless HEAD responses while explicit HEAD routes remain the ContractGraph/OpenAPI-visible contract.
