---
"create-croco-app": patch
"@croco/problems-core": patch
---

Keep scaffold destinations untouched until generation, Git initialization, dependency installation, and lockfile validation succeed, report machine-readable retry commands after failures, and keep the generated Problem registry synchronized with the new scaffold failure locations.
