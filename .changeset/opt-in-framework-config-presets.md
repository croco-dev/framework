---
"@croco/framework-config": major
"@croco/problems-core": patch
---

Validate only explicitly selected runtime environment presets, keep the default `env` app-only, and expose the previous all-integration configuration as `fullEnv` and `fullRuntimeEnvPresets`.
