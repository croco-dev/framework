---
"@croco/ratelimit-core": minor
---

Isolate rate-limit principals with versioned structured keys that preserve policy and segment boundaries across delimiters, routes, IPv6 addresses, Unicode, empty values, and missing values.
