---
"@croco/transports-http": patch
---

Runtime fetch paths now preserve provided execution context options so Node, Lambda, and Cloudflare Workers capability behavior can be verified against the shared runtime capability manifest.
