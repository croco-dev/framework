---
"@croco/tenant-core": patch
---

Keep tenant authorization outcomes authoritative when policy trace or isolation audit delivery fails by default, while exposing an explicit fail-closed mode for allowed decisions.
