---
"@croco/desktop-codegen": minor
"@croco/problems-core": patch
---

Generate window-scoped renderer clients whose inputs, results, Problems, events, and cancellation options are fixed by the desktop contract graph.
