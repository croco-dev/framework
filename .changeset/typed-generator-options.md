---
"create-croco-app": minor
---

Expose separate raw, normalized, and resolved generation option contracts so incompatible goal, preset, provider, hosting, deployment, and UI combinations fail at compile time while CLI input keeps its Problem-backed validation.
