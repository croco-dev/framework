---
"@croco/pagination-core": patch
---

Encode and decode cursor pagination tokens in browsers, workers, and edge runtimes without a Node `Buffer` polyfill while preserving existing base64url cursor strings.
