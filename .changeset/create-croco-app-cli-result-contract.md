---
"create-croco-app": patch
---

CLI runs now report structured diagnostics with recovery text and successful generation next-step commands, with `--json` output available for noninteractive consumers.
