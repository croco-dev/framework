---
"@croco/tenant-core": patch
---

Provide a tenant isolation enforcer that fails missing tenant context, default tenant fallback, unsafe cross-tenant queries, and unreasoned admin bypasses with typed Problems.
