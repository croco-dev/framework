---
"@croco/audit-core": patch
"@croco/problems-core": patch
---

Ignore client-controlled forwarding headers by default and resolve audit client IPs only through an explicit trusted-proxy hop policy.
