---
"@croco/cli": patch
---

Add `croco upgrade` to report version migration findings, show safe codemod diffs in dry-run mode, apply safe rewrites with `--write`, and leave uncertain changes as manual confirmations.
