---
"@croco/pagination-core": patch
"@croco/problems-core": patch
---

Reject repeated scalar pagination query parameters with one stable Problem across plain records and URLSearchParams while preserving single values and defaults, and register the new Problem code in the public registry.
