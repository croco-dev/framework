---
"@croco/protocols-graphql": patch
---

Normalize GraphQL decorator metadata ownership so contract snapshots and runtime policy readers use one inherited resolver-method contract without leaking derived declarations into base resolvers.
