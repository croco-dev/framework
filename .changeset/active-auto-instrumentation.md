---
"@croco/telemetry-sdk-node": patch
---

Apply configured Node and Lambda automatic instrumentations during SDK startup, reject unsupported settings explicitly, and report sanitized enabled module identities through telemetry diagnostics.
