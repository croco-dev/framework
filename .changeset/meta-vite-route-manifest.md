---
"@croco/meta-vite": patch
---

Meta Vite apps can now emit deterministic route manifests for page routes, API routes, server actions, component references, revalidation, and runtime capability requirements.
