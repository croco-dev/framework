paths:
  .github/workflows/release.yml:
    # GitHub supports concurrency.queue, but actionlint 1.7.12 has not shipped its schema update yet.
    ignore:
      - 'unexpected key "queue" for "concurrency" section'
