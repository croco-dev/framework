- 2026-04-22 Task 4: 보안 baseline 측정 결과 `pnpm audit:prod`는 104건(11 low / 47 moderate / 42 high / 4 critical)으로 실패했고, `lefthook.yaml`과 `.github/workflows/ci.yml` 어디에도 secret scanning/SAST 단계가 없었다. 보안 자동화 1차 후보는 단일 도구 기준 `gitleaks`가 가장 범위 적합했다.
- 2026-04-22 Task 5: `pnpm bench:check --output-json=benchmark-result.json`는 13.365초에 종료 코드 0으로 재현되었고 `benchmark-result.json`을 생성했다. `.github/workflows/benchmark.yml`의 현재 semantics는 `continue-on-error: true` 후 `if: steps.bench.outcome == 'failure'`에서 `exit 1`로 마무리되는 구조라 true warning-only가 아니라 artifact/comment 보존 뒤 hard fail하는 enforce형 gate다. `benchmarks/thresholds.json`에는 cold-start 계열 6개 항목 threshold가 없어 모두 `skip` 처리된다.
- 2026-04-22 Task 6: onboarding baseline 기준으로 루트 `.env.example`은 없고, README/AGENTS/CONTRIBUTING 어디에도 env contract가 없다. 신규 기여자 준비 흐름은 사실상 `pnpm install` 후 `pnpm build`, `pnpm test`, `pnpm typecheck`를 수동으로 이어야 하며, 루트 `prepare`는 `lefthook install`만 수행해 one-command setup 역할을 하지 못한다. 버전 계약은 `.nvmrc=22`, `engines.node>=22`, `packageManager=pnpm@10.15.1`, CONTRIBUTING의 Node 22+/pnpm 10+로 분산 문서화되어 있다.
- 2026-04-22 Task 7: Task 2 baseline 명령 `npx --yes madge --circular --extensions ts packages`는 종료 코드 1과 함께 5건 순환 의존을 보고한다. warning-only 승격은 명령 자체를 바꾸지 않고 루트 script(`pnpm architecture:check:circular`)로 고정한 뒤 GitHub Actions step에 `continue-on-error: true`를 적용하는 방식이 가장 단순하며, validate job의 후속 품질 단계 흐름을 유지한다. 로컬 재현 시간은 약 2.9초였다.
- 2026-04-22 Task 8: core coverage warning-only는 기존 `pnpm test:coverage:core`를 그대로 재사용하고, `vitest.config.ts`에 `json-summary` reporter를 추가해 패키지별 `coverage-summary.json`을 읽는 방식이 가장 단순했다. 새 `pnpm test:coverage:core:warning` 스크립트는 baseline 대비 하락과 threshold 미달을 리포트로 남기되 종료 코드는 항상 0으로 유지했다. 로컬 실행 시간은 약 2.0초였다.
- 2026-04-22 Task 9: 보안 warning-only 1차 도입은 기존 hard gate `pnpm audit:prod`를 유지한 채 validate job 앞단에 `continue-on-error: true` warning step를 추가하고, 신규 도구는 `gitleaks` 하나만 붙이는 구성이 가장 범위 적합했다. `ci-reports/security` artifact에 audit 로그, gitleaks SARIF/text, false positive 관리 방안, 실행 시간 baseline을 함께 남기면 enforce 전환 판단 근거를 한 곳에 모을 수 있다.
- 2026-04-22 Task 12: enforce 전환 문서는 각 축의 현재 backlog와 warning-only 안정화 수준을 분리해서 써야 했다. 특히 Architecture(순환 5건), Security(104건 취약점), Performance(skip 6건)처럼 baseline debt가 열린 축은 "언제 enforce 가능한가"와 "지금 왜 안 되는가"를 함께 적어야 조기 승격을 막을 수 있었다.
