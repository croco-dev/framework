Task 2: 아키텍처 baseline 측정

시작 시각: 2026-04-22 12:39:43 KST
종료 시각: 2026-04-22 12:41:32 KST
작업 브랜치: framework-tech-health-enforcement

## 실행 근거
- AGENTS.md 4계층 구조 기준: Framework → Protocols → Transports → Integrations
- AGENTS.md repository-core 규칙: `drizzle-orm`, `@croco/tx-drizzle` 직접 의존 금지
- 참고 리포트: `.sisyphus/tech-improve/framework-20260421.md`

## 도구 확인
- `pnpm ls madge dependency-cruiser --depth -1`
  - 출력 없음(루트 설치 확인 실패)
- `package.json`에서 `madge`, `dependency-cruiser`, `depcruise` devDependency 검색
  - 결과: 없음
- 대체 실행: `npx --yes madge --circular --extensions ts packages`
  - 결과: 성공

## 순환 의존 측정 결과
`npx --yes madge --circular --extensions ts packages`

Processed 1206 files (2.4s) (30 warnings)

발견된 순환 의존: 총 5건

1. `problems-core/src/libs/Problem.ts`
   → `problems-core/src/libs/ProblemExtensions.ts`
   → `problems-core/src/libs/problems/InvalidExtensionsProblem.ts`

2. `events-core/src/libs/EventBus.ts`
   → `events-core/src/libs/EventHandler.ts`

3. `events-core/src/libs/EventBus.ts`
   → `events-core/src/libs/interfaces/EventSubscribing.ts`

4. `auth-core/src/libs/interfaces/AbstractRoleRegistry.ts`
   → `auth-core/src/libs/rbac/Role.ts`

5. `migration-runner/src/libs/types.ts`
   → `migration-runner/src/libs/MigrationStore.ts`

## 이전 진단과의 대조
`.sisyphus/tech-improve/framework-20260421.md`의 Architecture 섹션은 intra-package 순환 의존 5건을 보고했다.

- 일치: `problems-core`, `events-core`(2건), `auth-core`, `migration-runner`
- 차이: 리포트에 있던 `tx-drizzle/src/libs/RlsTxAdapter.ts → 관련 types` 순환은 이번 madge 실행 결과에 포함되지 않음

해석:
- baseline 재측정 기준으로는 현재 자동 검출 가능한 순환 의존이 5건이다.
- 기존 리포트의 5건과 패키지 수는 같지만 구성은 일부 달라졌다.
- 후속 warning-only / enforce 작업에서는 현재 측정값(5건)과 리포트 차이를 함께 보존해야 한다.

## 레이어 경계 확인 근거
패키지 manifest 기반 표본 확인:

- `packages/protocols-rest/package.json`
  - `@croco/framework-context`, `@croco/problems-core` 의존만 존재
  - `@croco/transports-*` 의존 없음

- `packages/protocols-graphql/package.json`
  - `@croco/framework-context` 의존 존재
  - `@croco/transports-*` 의존 없음

- `packages/transports-http/package.json`
  - `@croco/protocols-rest` 의존 존재
  - Framework/Protocols 방향과 일치

- `packages/transports-graphql/package.json`
  - `@croco/protocols-graphql` 의존 존재
  - Framework/Protocols 방향과 일치

- `packages/transports-cloudflare-workers/package.json`
  - `@croco/transports-http` 의존 존재
  - transport 간 의존은 존재하지만 protocol이 transport를 역참조하는 패턴은 이번 표본에서 확인되지 않음

- `packages/integrations-posthog/package.json`
  - `@croco/framework-context` 의존 존재
  - `@croco/protocols-*`, `@croco/transports-*` 의존 없음

추가 grep 결과:
- `packages/protocols-*/package.json` 내 `@croco/transports-*`: 0건
- `packages/framework-*/package.json` 내 `@croco/(protocols|transports|integrations)-*`: 0건
- `packages/integrations-*/package.json` 내 `@croco/(framework|protocols|transports)-*`: `integrations-posthog -> @croco/framework-context` 1건만 확인

판정:
- 4계층 구조를 정면으로 위반하는 명백한 package.json 레벨 역방향 의존은 이번 baseline 표본에서는 확인되지 않았다.
- 다만 CI 강제 규칙이 없으므로 현재 상태는 “문서화된 규칙이 대체로 지켜지는 상태”이며, 자동 차단 상태는 아니다.

## repository-core 의존성 규칙 확인
대상: `packages/repository-core/src`

grep 패턴:
- `from '@croco/tx-drizzle'`
- `from 'drizzle-orm'`
- `require('@croco/tx-drizzle')`
- `require('drizzle-orm')`

결과: 0건

판정:
- AGENTS.md의 repository-core 오염 금지 규칙은 현재 baseline에서 위반이 검출되지 않았다.

## 결론
- 현재 모노레포 baseline에서 자동 검출된 순환 의존은 5건이다.
- 4계층 규칙은 package manifest 표본과 grep 기준으로는 대체로 유지되고 있다.
- repository-core의 drizzle 관련 금지 의존은 검출되지 않았다.
- 후속 작업의 핵심은 순환 제거가 아니라, 현재 측정값을 warning-only / enforce 게이트로 승격하는 것이다.
