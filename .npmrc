save-exact=true
# Keep the hoisted linker for current monorepo tool compatibility. Publishable
# package dependency correctness is guarded by package-manifests:check plus
# packed-package smoke tests that install tarballs in isolated, unhoisted
# consumers.
node-linker=hoisted
