---
editUrl: false
next: false
prev: false
title: "ENGAGEMENT_PERMISSIONS"
---

> `const` **ENGAGEMENT_PERMISSIONS**: `object`

## Type Declaration

### AUDIENCE_READ

> `readonly` **AUDIENCE_READ**: `"engagement:audience:read"` = `"engagement:audience:read"`

### CAMPAIGN_CANCEL

> `readonly` **CAMPAIGN_CANCEL**: `"engagement:campaign:cancel"` = `"engagement:campaign:cancel"`

### CAMPAIGN_RUN

> `readonly` **CAMPAIGN_RUN**: `"engagement:campaign:run"` = `"engagement:campaign:run"`

### CUSTOMER_READ

> `readonly` **CUSTOMER_READ**: `"engagement:customer:read"` = `"engagement:customer:read"`

### DELIVERY_READ

> `readonly` **DELIVERY_READ**: `"engagement:delivery:read"` = `"engagement:delivery:read"`

### ENDPOINT_REACTIVATE

> `readonly` **ENDPOINT_REACTIVATE**: `"engagement:endpoint:reactivate"` = `"engagement:endpoint:reactivate"`

### MESSAGE_PREVIEW

> `readonly` **MESSAGE_PREVIEW**: `"engagement:message:preview"` = `"engagement:message:preview"`

### MESSAGE_TEST_SEND

> `readonly` **MESSAGE_TEST_SEND**: `"engagement:message:test-send"` = `"engagement:message:test-send"`

### PII_READ

> `readonly` **PII_READ**: `"engagement:pii:read"` = `"engagement:pii:read"`

### SUPPRESSION_WRITE

> `readonly` **SUPPRESSION_WRITE**: `"engagement:suppression:write"` = `"engagement:suppression:write"`
