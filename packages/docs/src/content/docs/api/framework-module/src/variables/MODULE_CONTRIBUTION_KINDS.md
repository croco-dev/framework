---
editUrl: false
next: false
prev: false
title: "MODULE_CONTRIBUTION_KINDS"
---

> `const` **MODULE_CONTRIBUTION_KINDS**: `object`

## Type Declaration

### diagnosticsProvider

> `readonly` **diagnosticsProvider**: `"diagnostics.provider"` = `"diagnostics.provider"`

### eventHandler

> `readonly` **eventHandler**: `"event.handler"` = `"event.handler"`

### httpController

> `readonly` **httpController**: `"http.controller"` = `"http.controller"`

### httpMiddleware

> `readonly` **httpMiddleware**: `"http.middleware"` = `"http.middleware"`

### httpRoute

> `readonly` **httpRoute**: `"http.route"` = `"http.route"`

### lifecycleResource

> `readonly` **lifecycleResource**: `"lifecycle.resource"` = `"lifecycle.resource"`

### taskHandler

> `readonly` **taskHandler**: `"task.handler"` = `"task.handler"`

### triggerHandler

> `readonly` **triggerHandler**: `"trigger.handler"` = `"trigger.handler"`
