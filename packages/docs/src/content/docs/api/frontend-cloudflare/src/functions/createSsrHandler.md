---
editUrl: false
next: false
prev: false
title: "createSsrHandler"
---

> **createSsrHandler**(`options?`): (`request`, `env`, `ctx`) => `Promise`\<`Response`\>

Creates a Cloudflare Workers SSR handler using meta-vite's RenderServer.

## Parameters

### options?

`CloudflareSsrHandlerOptions` = `{}`

## Returns

(`request`, `env`, `ctx`) => `Promise`\<`Response`\>
