---
title: Problem Recovery Cookbook
description: Generated Croco Problem code registry with recovery and telemetry metadata.
---

# Problem Recovery Cookbook

> Generated by `pnpm problem-registry:write`. Do not edit this file by hand.

This cookbook documents 358 public Croco Problem codes. The deterministic JSON registry is generated at `docs/problem-code-registry.json`.

## Index

| Code                                                                                                                | Category              | Status | Retryability  | Redaction     | Sources |
| ------------------------------------------------------------------------------------------------------------------- | --------------------- | -----: | ------------- | ------------- | ------: |
| [`ACCESS_DENIED`](#access-denied)                                                                                   | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`access-core/forbidden`](#access-core-forbidden)                                                                   | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`admin-console/user-not-found`](#admin-console-user-not-found)                                                     | NotFound              |    404 | not-retryable | public        |       1 |
| [`admin-core/resource-validation-failed`](#admin-core-resource-validation-failed)                                   | ValidationError       |    422 | not-retryable | public        |       1 |
| [`admin-generated/contract-diagnostics`](#admin-generated-contract-diagnostics)                                     | ValidationError       |    422 | not-retryable | public        |       1 |
| [`ai-saas/model-not-found`](#ai-saas-model-not-found)                                                               | NotFound              |    404 | not-retryable | public        |       1 |
| [`ai-saas/model-required`](#ai-saas-model-required)                                                                 | ValidationError       |    422 | not-retryable | public        |       1 |
| [`ai-saas/provider-unavailable`](#ai-saas-provider-unavailable)                                                     | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`ai-saas/quota-exceeded`](#ai-saas-quota-exceeded)                                                                 | TooManyRequests       |    429 | retryable     | safe-message  |       1 |
| [`ai-saas/rate-limit-exceeded`](#ai-saas-rate-limit-exceeded)                                                       | TooManyRequests       |    429 | retryable     | safe-message  |       1 |
| [`ai-saas/smoke-failed`](#ai-saas-smoke-failed)                                                                     | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`ai-saas/tenant-not-found`](#ai-saas-tenant-not-found)                                                             | NotFound              |    404 | not-retryable | public        |       1 |
| [`ai-saas/tenant-required`](#ai-saas-tenant-required)                                                               | ValidationError       |    422 | not-retryable | public        |       1 |
| [`ALREADY_MEMBER`](#already-member)                                                                                 | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`API_KEY_EXPIRED`](#api-key-expired)                                                                               | Unauthorized          |    401 | not-retryable | safe-message  |       1 |
| [`API_KEY_NOT_FOUND`](#api-key-not-found)                                                                           | NotFound              |    404 | not-retryable | public        |       1 |
| [`API_KEY_REVOKED`](#api-key-revoked)                                                                               | Unauthorized          |    401 | not-retryable | safe-message  |       1 |
| [`architecture-policy/manifest-json-parse`](#architecture-policy-manifest-json-parse)                               | ValidationError       |    422 | not-retryable | public        |       1 |
| [`architecture-policy/manifest-schema-version`](#architecture-policy-manifest-schema-version)                       | ValidationError       |    422 | not-retryable | public        |       1 |
| [`architecture-policy/manifest-shape`](#architecture-policy-manifest-shape)                                         | ValidationError       |    422 | not-retryable | public        |       1 |
| [`architecture-policy/package-json-parse`](#architecture-policy-package-json-parse)                                 | ValidationError       |    422 | not-retryable | public        |       1 |
| [`audit-core/auditable-decorator-misuse`](#audit-core-auditable-decorator-misuse)                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`audit/insert-failed`](#audit-insert-failed)                                                                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`auth-better-auth/invalid-session-payload`](#auth-better-auth-invalid-session-payload)                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`auth-better-auth/invalid-webhook-payload`](#auth-better-auth-invalid-webhook-payload)                             | BadRequest            |    400 | not-retryable | public        |       1 |
| [`auth-better-auth/invalid-webhook-signature`](#auth-better-auth-invalid-webhook-signature)                         | Unauthorized          |    401 | not-retryable | safe-message  |       1 |
| [`auth-better-auth/not-initialized`](#auth-better-auth-not-initialized)                                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`auth-better-auth/session-lookup-failed`](#auth-better-auth-session-lookup-failed)                                 | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`auth-better-auth/session-not-found`](#auth-better-auth-session-not-found)                                         | NotFound              |    404 | not-retryable | public        |       1 |
| [`auth-better-auth/user-not-found`](#auth-better-auth-user-not-found)                                               | NotFound              |    404 | not-retryable | public        |       1 |
| [`auth-clerk/duplicate-tenant-mapping`](#auth-clerk-duplicate-tenant-mapping)                                       | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`auth-clerk/external-service-error`](#auth-clerk-external-service-error)                                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`auth-clerk/invalid-webhook-payload`](#auth-clerk-invalid-webhook-payload)                                         | ValidationError       |    422 | not-retryable | public        |       1 |
| [`auth-clerk/malformed-claim`](#auth-clerk-malformed-claim)                                                         | Unauthorized          |    401 | not-retryable | safe-message  |       1 |
| [`auth-clerk/public-user-data-missing`](#auth-clerk-public-user-data-missing)                                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`auth-clerk/token-verification-failed`](#auth-clerk-token-verification-failed)                                     | Unauthorized          |    401 | not-retryable | safe-message  |       1 |
| [`auth-clerk/webhook-verification-failed`](#auth-clerk-webhook-verification-failed)                                 | Unauthorized          |    401 | not-retryable | safe-message  |       1 |
| [`auth-core/api-key-creation-failed`](#auth-core-api-key-creation-failed)                                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`auth-core/invalid-permission-action`](#auth-core-invalid-permission-action)                                       | ValidationError       |    422 | not-retryable | public        |       1 |
| [`auth-core/invalid-permission-format`](#auth-core-invalid-permission-format)                                       | ValidationError       |    422 | not-retryable | public        |       1 |
| [`BAD_REQUEST`](#bad-request)                                                                                       | BadRequest            |    400 | not-retryable | public        |       1 |
| [`BILLING_STATUS_MAPPING_FAILED`](#billing-status-mapping-failed)                                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`billing-polar/customer-not-found`](#billing-polar-customer-not-found)                                             | NotFound              |    404 | not-retryable | public        |       1 |
| [`billing-polar/missing-config`](#billing-polar-missing-config)                                                     | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`billing-polar/retryable-upstream`](#billing-polar-retryable-upstream)                                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`billing-polar/subscription-not-found`](#billing-polar-subscription-not-found)                                     | NotFound              |    404 | not-retryable | public        |       1 |
| [`billing-polar/terminal-upstream`](#billing-polar-terminal-upstream)                                               | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`billing-polar/validation-failed`](#billing-polar-validation-failed)                                               | ValidationError       |    422 | not-retryable | public        |       1 |
| [`billing/account-not-found`](#billing-account-not-found)                                                           | NotFound              |    404 | not-retryable | public        |       1 |
| [`billing/checkout-creation-failed`](#billing-checkout-creation-failed)                                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`billing/invalid-money-amount`](#billing-invalid-money-amount)                                                     | BadRequest            |    400 | not-retryable | public        |       1 |
| [`billing/invalid-money-currency`](#billing-invalid-money-currency)                                                 | BadRequest            |    400 | not-retryable | public        |       1 |
| [`billing/money-currency-mismatch`](#billing-money-currency-mismatch)                                               | BusinessRuleViolation |    422 | conditional   | safe-message  |       1 |
| [`billing/money-division-by-zero`](#billing-money-division-by-zero)                                                 | BadRequest            |    400 | not-retryable | public        |       1 |
| [`billing/subscription-not-found`](#billing-subscription-not-found)                                                 | NotFound              |    404 | not-retryable | public        |       1 |
| [`billing/webhook-already-processed`](#billing-webhook-already-processed)                                           | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`BLOCKED_DURING_IMPERSONATION`](#blocked-during-impersonation)                                                     | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`cache-core/invalid-decorator-config`](#cache-core-invalid-decorator-config)                                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`CIRCUIT_BREAKER_OPEN`](#circuit-breaker-open)                                                                     | TooManyRequests       |    429 | retryable     | safe-message  |       1 |
| [`cli/invalid-jobs-number`](#cli-invalid-jobs-number)                                                               | BadRequest            |    400 | not-retryable | public        |       1 |
| [`cli/invalid-jobs-target-url`](#cli-invalid-jobs-target-url)                                                       | BadRequest            |    400 | not-retryable | public        |       1 |
| [`cli/invalid-ops-target-url`](#cli-invalid-ops-target-url)                                                         | BadRequest            |    400 | not-retryable | public        |       1 |
| [`cli/invalid-ops-timeout`](#cli-invalid-ops-timeout)                                                               | BadRequest            |    400 | not-retryable | public        |       1 |
| [`cli/missing-jobs-target-url`](#cli-missing-jobs-target-url)                                                       | BadRequest            |    400 | not-retryable | public        |       1 |
| [`cloudflare/images-invalid-ttl`](#cloudflare-images-invalid-ttl)                                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`cloudflare/images-null-result`](#cloudflare-images-null-result)                                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`cloudflare/images-upload-intent-null-result`](#cloudflare-images-upload-intent-null-result)                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`CONFLICTING_PAGINATION`](#conflicting-pagination)                                                                 | BadRequest            |    400 | not-retryable | public        |       1 |
| [`create-croco-app/directory-not-empty`](#create-croco-app-directory-not-empty)                                     | ValidationError       |    422 | not-retryable | public        |       1 |
| [`create-croco-app/invalid-cli-option`](#create-croco-app-invalid-cli-option)                                       | ValidationError       |    422 | not-retryable | public        |       1 |
| [`create-croco-app/invalid-goal-option`](#create-croco-app-invalid-goal-option)                                     | ValidationError       |    422 | not-retryable | public        |       1 |
| [`create-croco-app/invalid-saas-preset-option`](#create-croco-app-invalid-saas-preset-option)                       | ValidationError       |    422 | not-retryable | public        |       1 |
| [`create-croco-app/unexpected-failure`](#create-croco-app-unexpected-failure)                                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`dataloader-core/batch-result-length-mismatch`](#dataloader-core-batch-result-length-mismatch)                     | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`diagnostics-core/duplicate-provider`](#diagnostics-core-duplicate-provider)                                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`DUPLICATE_INVITATION`](#duplicate-invitation)                                                                     | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`DUPLICATE_RECOVER_HANDLER`](#duplicate-recover-handler)                                                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`DURATION_PARSE_ERROR`](#duration-parse-error)                                                                     | BadRequest            |    400 | not-retryable | public        |       1 |
| [`EMBEDDING_ERROR`](#embedding-error)                                                                               | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`ENTITLEMENT_DENIED`](#entitlement-denied)                                                                         | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`ENTITLEMENT_INACTIVE_SUBSCRIPTION`](#entitlement-inactive-subscription)                                           | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`ENTITLEMENT_MISSING_PLAN`](#entitlement-missing-plan)                                                             | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`ENTITLEMENT_NOT_FOUND`](#entitlement-not-found)                                                                   | NotFound              |    404 | not-retryable | public        |       1 |
| [`ENTITLEMENT_PROVIDER_UNAVAILABLE`](#entitlement-provider-unavailable)                                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`ENTITLEMENT_QUOTA_EXCEEDED`](#entitlement-quota-exceeded)                                                         | TooManyRequests       |    429 | retryable     | safe-message  |       1 |
| [`ENTITLEMENT_REQUIREMENT_INVALID`](#entitlement-requirement-invalid)                                               | ValidationError       |    422 | not-retryable | public        |       1 |
| [`events-core/after-commit-requires-active-transaction`](#events-core-after-commit-requires-active-transaction)     | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`events-core/deserialization-error`](#events-core-deserialization-error)                                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`events-core/duplicate-event-field`](#events-core-duplicate-event-field)                                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`events-core/duplicate-event-name`](#events-core-duplicate-event-name)                                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`events-core/event-bus-not-set`](#events-core-event-bus-not-set)                                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`events-core/event-definition-error`](#events-core-event-definition-error)                                         | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`events-core/transaction-context-unavailable`](#events-core-transaction-context-unavailable)                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`events-core/unknown-event-type`](#events-core-unknown-event-type)                                                 | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`events-inmemory/backpressure-exceeded`](#events-inmemory-backpressure-exceeded)                                   | TooManyRequests       |    429 | retryable     | safe-message  |       1 |
| [`events-inmemory/backpressure-timeout`](#events-inmemory-backpressure-timeout)                                     | TooManyRequests       |    429 | retryable     | safe-message  |       1 |
| [`events-inmemory/invalid-configuration`](#events-inmemory-invalid-configuration)                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`events-inmemory/publish-failed`](#events-inmemory-publish-failed)                                                 | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`events-tx/outbox-publish-exhausted`](#events-tx-outbox-publish-exhausted)                                         | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`events-tx/outbox-transaction-required`](#events-tx-outbox-transaction-required)                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`events-tx/storage-error`](#events-tx-storage-error)                                                               | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`events-tx/transaction-state-error`](#events-tx-transaction-state-error)                                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`execution/conflict`](#execution-conflict)                                                                         | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`execution/invalid-state-transition`](#execution-invalid-state-transition)                                         | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`execution/max-retries-exceeded`](#execution-max-retries-exceeded)                                                 | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`execution/not-found`](#execution-not-found)                                                                       | NotFound              |    404 | not-retryable | public        |       1 |
| [`FORBIDDEN`](#forbidden)                                                                                           | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`framework-config/config-schema-not-found`](#framework-config-config-schema-not-found)                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`framework-config/config-validation-failed`](#framework-config-config-validation-failed)                           | ValidationError       |    422 | not-retryable | public        |       1 |
| [`framework-config/invalid-boolean-env`](#framework-config-invalid-boolean-env)                                     | ValidationError       |    422 | not-retryable | public        |       1 |
| [`framework-context/circular-dependency`](#framework-context-circular-dependency)                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`framework-context/context-middleware-execution-error`](#framework-context-context-middleware-execution-error)     | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`framework-context/di-resolution-failed`](#framework-context-di-resolution-failed)                                 | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`framework-context/di-scope-mismatch`](#framework-context-di-scope-mismatch)                                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`framework-context/pipeline-graph-invalid`](#framework-context-pipeline-graph-invalid)                             | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`framework-context/policy-capability-unavailable`](#framework-context-policy-capability-unavailable)               | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`framework-context/policy-conflict`](#framework-context-policy-conflict)                                           | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`framework-context/policy-definition-invalid`](#framework-context-policy-definition-invalid)                       | ValidationError       |    422 | not-retryable | public        |       1 |
| [`framework-context/request-scope-outside-context`](#framework-context-request-scope-outside-context)               | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`framework-context/shutdown-configuration-conflict`](#framework-context-shutdown-configuration-conflict)           | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`framework-context/shutdown-timeout`](#framework-context-shutdown-timeout)                                         | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`framework-module/circular-dependency`](#framework-module-circular-dependency)                                     | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`framework-module/invalid-module-definition`](#framework-module-invalid-module-definition)                         | ValidationError       |    422 | not-retryable | public        |       1 |
| [`framework-module/lifecycle-failed`](#framework-module-lifecycle-failed)                                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`framework-module/provider-not-visible`](#framework-module-provider-not-visible)                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`frontend-vite/missing-cloudflare-vite-plugin`](#frontend-vite-missing-cloudflare-vite-plugin)                     | ValidationError       |    422 | not-retryable | public        |       1 |
| [`GENERATION_ERROR`](#generation-error)                                                                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`gid-core/id-type-only-property`](#gid-core-id-type-only-property)                                                 | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`gid-core/invalid-id-prefix`](#gid-core-invalid-id-prefix)                                                         | ValidationError       |    422 | not-retryable | public        |       1 |
| [`governance-core/delete-not-supported`](#governance-core-delete-not-supported)                                     | NotImplemented        |    501 | not-retryable | public        |       1 |
| [`governance-core/export-not-supported`](#governance-core-export-not-supported)                                     | NotImplemented        |    501 | not-retryable | public        |       1 |
| [`governance-core/resource-validation-failed`](#governance-core-resource-validation-failed)                         | ValidationError       |    422 | not-retryable | public        |       1 |
| [`governance-core/retention-policy-violation`](#governance-core-retention-policy-violation)                         | BusinessRuleViolation |    422 | conditional   | safe-message  |       1 |
| [`GRAPHQL_NOT_FOUND`](#graphql-not-found)                                                                           | NotFound              |    404 | not-retryable | public        |       1 |
| [`HEALTH_SCORE_NOT_FOUND`](#health-score-not-found)                                                                 | NotFound              |    404 | not-retryable | public        |       1 |
| [`idempotency-core/invalid-key`](#idempotency-core-invalid-key)                                                     | BadRequest            |    400 | not-retryable | public        |       1 |
| [`idempotency-core/key-conflict`](#idempotency-core-key-conflict)                                                   | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`idempotency-core/reservation-not-found`](#idempotency-core-reservation-not-found)                                 | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`idempotency-core/reservation-state`](#idempotency-core-reservation-state)                                         | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`IMPERSONATION_REASON_REQUIRED`](#impersonation-reason-required)                                                   | BadRequest            |    400 | not-retryable | public        |       1 |
| [`IMPERSONATION_SESSION_NOT_FOUND`](#impersonation-session-not-found)                                               | NotFound              |    404 | not-retryable | public        |       1 |
| [`INDEX_NOT_FOUND`](#index-not-found)                                                                               | NotFound              |    404 | not-retryable | public        |       1 |
| [`integrations-posthog/missing-config`](#integrations-posthog-missing-config)                                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`INVALID_AUTO_JOIN_ROLE`](#invalid-auto-join-role)                                                                 | BadRequest            |    400 | not-retryable | public        |       1 |
| [`INVALID_CURSOR`](#invalid-cursor)                                                                                 | BadRequest            |    400 | not-retryable | public        |       1 |
| [`INVALID_RETRY_CONFIGURATION`](#invalid-retry-configuration)                                                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`INVALID_ROLE`](#invalid-role)                                                                                     | BadRequest            |    400 | not-retryable | public        |       1 |
| [`INVITATION_ALREADY_ACCEPTED`](#invitation-already-accepted)                                                       | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`INVITATION_EMAIL_MISMATCH`](#invitation-email-mismatch)                                                           | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`INVITATION_EXPIRED`](#invitation-expired)                                                                         | Gone                  |    410 | not-retryable | public        |       1 |
| [`INVITATION_INVALID_STATUS`](#invitation-invalid-status)                                                           | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`INVITATION_NOT_FOUND`](#invitation-not-found)                                                                     | NotFound              |    404 | not-retryable | public        |       1 |
| [`INVITATION_RATE_LIMIT_EXCEEDED`](#invitation-rate-limit-exceeded)                                                 | TooManyRequests       |    429 | retryable     | safe-message  |       1 |
| [`invitation-core/batch-size-exceeded`](#invitation-core-batch-size-exceeded)                                       | BadRequest            |    400 | not-retryable | public        |       1 |
| [`LAMBDA_TIMEOUT_GUARD`](#lambda-timeout-guard)                                                                     | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`LAST_OWNER`](#last-owner)                                                                                         | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`lifecycle-core/action-adapter-failed`](#lifecycle-core-action-adapter-failed)                                     | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`lifecycle-core/duplicate-rule`](#lifecycle-core-duplicate-rule)                                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`lifecycle-core/rule-definition-invalid`](#lifecycle-core-rule-definition-invalid)                                 | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`LLM_PROVIDER_NOT_FOUND`](#llm-provider-not-found)                                                                 | NotFound              |    404 | not-retryable | public        |       1 |
| [`LLM_SERVICE_ERROR`](#llm-service-error)                                                                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`llm-core/invalid-llm-prompt`](#llm-core-invalid-llm-prompt)                                                       | BadRequest            |    400 | not-retryable | public        |       1 |
| [`llm-core/invalid-llm-response`](#llm-core-invalid-llm-response)                                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`llm-core/llm-service-not-initialized`](#llm-core-llm-service-not-initialized)                                     | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`llm-core/rate-limit-exceeded`](#llm-core-rate-limit-exceeded)                                                     | TooManyRequests       |    429 | retryable     | safe-message  |       1 |
| [`llm-metering/cost-limit-exceeded`](#llm-metering-cost-limit-exceeded)                                             | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`llm-metering/pricing-not-found`](#llm-metering-pricing-not-found)                                                 | NotFound              |    404 | not-retryable | public        |       1 |
| [`llm-metering/pricing-registry-conflict`](#llm-metering-pricing-registry-conflict)                                 | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`llm-metering/quota-exceeded`](#llm-metering-quota-exceeded)                                                       | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`llm-metering/record-failed`](#llm-metering-record-failed)                                                         | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`llm-openai/aborted`](#llm-openai-aborted)                                                                         | BadRequest            |    400 | not-retryable | public        |       1 |
| [`llm-openai/authentication-failed`](#llm-openai-authentication-failed)                                             | Unauthorized          |    401 | not-retryable | safe-message  |       1 |
| [`llm-openai/invalid-response`](#llm-openai-invalid-response)                                                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`llm-openai/missing-config`](#llm-openai-missing-config)                                                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`llm-openai/rate-limited`](#llm-openai-rate-limited)                                                               | TooManyRequests       |    429 | retryable     | safe-message  |       1 |
| [`llm-openai/retryable-upstream`](#llm-openai-retryable-upstream)                                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`llm-openai/terminal-upstream`](#llm-openai-terminal-upstream)                                                     | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`llm-openai/validation-failed`](#llm-openai-validation-failed)                                                     | BadRequest            |    400 | not-retryable | public        |       1 |
| [`MEMBERSHIP_CONSTRAINT`](#membership-constraint)                                                                   | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`MEMBERSHIP_NOT_FOUND`](#membership-not-found)                                                                     | NotFound              |    404 | not-retryable | public        |       1 |
| [`meta-vite/server-action-invalid-path`](#meta-vite-server-action-invalid-path)                                     | BadRequest            |    400 | not-retryable | public        |       1 |
| [`meta-vite/server-action-not-found`](#meta-vite-server-action-not-found)                                           | NotFound              |    404 | not-retryable | public        |       1 |
| [`meta-vite/server-action-validation-failed`](#meta-vite-server-action-validation-failed)                           | ValidationError       |    422 | not-retryable | public        |       1 |
| [`meter/insert-failed`](#meter-insert-failed)                                                                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`metering/atomic-quota-not-supported`](#metering-atomic-quota-not-supported)                                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`metering/duplicate-record`](#metering-duplicate-record)                                                           | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`metering/invalid-meter`](#metering-invalid-meter)                                                                 | NotFound              |    404 | not-retryable | public        |       1 |
| [`metering/quota-exceeded`](#metering-quota-exceeded)                                                               | TooManyRequests       |    429 | retryable     | safe-message  |       1 |
| [`metering/redis-error`](#metering-redis-error)                                                                     | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`metrics-core/carrying-capacity-simulation-error`](#metrics-core-carrying-capacity-simulation-error)               | BusinessRuleViolation |    422 | conditional   | safe-message  |       1 |
| [`metrics-core/carrying-capacity-tenant-required`](#metrics-core-carrying-capacity-tenant-required)                 | ValidationError       |    422 | not-retryable | public        |       1 |
| [`metrics-core/gross-margin-required`](#metrics-core-gross-margin-required)                                         | ValidationError       |    422 | not-retryable | public        |       1 |
| [`metrics-core/mixed-currency-mrr`](#metrics-core-mixed-currency-mrr)                                               | ValidationError       |    422 | not-retryable | public        |       1 |
| [`metrics-core/retention-metrics-unavailable`](#metrics-core-retention-metrics-unavailable)                         | NotImplemented        |    501 | not-retryable | public        |       1 |
| [`metrics-core/snapshot-tenant-required`](#metrics-core-snapshot-tenant-required)                                   | ValidationError       |    422 | not-retryable | public        |       1 |
| [`MIDDLEWARE_EXECUTION_ERROR`](#middleware-execution-error)                                                         | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`migration-runner/database-url-required`](#migration-runner-database-url-required)                                 | BadRequest            |    400 | not-retryable | public        |       1 |
| [`migration-runner/invalid-count`](#migration-runner-invalid-count)                                                 | BadRequest            |    400 | not-retryable | public        |       1 |
| [`migration-runner/missing-down-function`](#migration-runner-missing-down-function)                                 | ValidationError       |    422 | not-retryable | public        |       1 |
| [`migration-runner/missing-up-function`](#migration-runner-missing-up-function)                                     | ValidationError       |    422 | not-retryable | public        |       1 |
| [`migration-runner/transaction-required`](#migration-runner-transaction-required)                                   | ValidationError       |    422 | not-retryable | public        |       1 |
| [`migration-runner/unsupported-dialect`](#migration-runner-unsupported-dialect)                                     | BadRequest            |    400 | not-retryable | public        |       1 |
| [`migration-runner/unsupported-query-result`](#migration-runner-unsupported-query-result)                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`MISSING_TENANT`](#missing-tenant)                                                                                 | BadRequest            |    400 | not-retryable | public        |       1 |
| [`MODEL_NOT_FOUND`](#model-not-found)                                                                               | NotFound              |    404 | not-retryable | public        |       1 |
| [`NESTED_IMPERSONATION_NOT_ALLOWED`](#nested-impersonation-not-allowed)                                             | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`notifications-core/default-provider-conflict`](#notifications-core-default-provider-conflict)                     | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`notifications-core/delivery-failed`](#notifications-core-delivery-failed)                                         | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`notifications-core/provider-already-registered`](#notifications-core-provider-already-registered)                 | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`notifications-core/provider-channel-mismatch`](#notifications-core-provider-channel-mismatch)                     | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`notifications-core/provider-not-configured`](#notifications-core-provider-not-configured)                         | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`notifications-core/provider-not-found`](#notifications-core-provider-not-found)                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`notifications-core/provider-not-registered`](#notifications-core-provider-not-registered)                         | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`notifications-core/send-max-attempts-invalid`](#notifications-core-send-max-attempts-invalid)                     | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`onboarding/context-required`](#onboarding-context-required)                                                       | Unauthorized          |    401 | not-retryable | safe-message  |       1 |
| [`onboarding/definition-not-found`](#onboarding-definition-not-found)                                               | NotFound              |    404 | not-retryable | public        |       1 |
| [`onboarding/step-not-found`](#onboarding-step-not-found)                                                           | NotFound              |    404 | not-retryable | public        |       1 |
| [`openapi-spec/controller-typescript-diagnostics`](#openapi-spec-controller-typescript-diagnostics)                 | ValidationError       |    422 | not-retryable | public        |       1 |
| [`openapi-spec/invalid-contract`](#openapi-spec-invalid-contract)                                                   | ValidationError       |    422 | not-retryable | public        |       1 |
| [`openapi-spec/no-rest-controllers-found`](#openapi-spec-no-rest-controllers-found)                                 | BadRequest            |    400 | not-retryable | public        |       1 |
| [`OTLP_ENDPOINT_REQUIRED`](#otlp-endpoint-required)                                                                 | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`OWNERSHIP_TRANSFER_REQUIRED`](#ownership-transfer-required)                                                       | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`problems-core/invalid-extensions`](#problems-core-invalid-extensions)                                             | BadRequest            |    400 | not-retryable | public        |       1 |
| [`problems-core/parse-error`](#problems-core-parse-error)                                                           | BadRequest            |    400 | not-retryable | public        |       1 |
| [`problems-core/problem-registry-invalid`](#problems-core-problem-registry-invalid)                                 | ValidationError       |    422 | not-retryable | public        |       1 |
| [`problems-core/unhandled-category`](#problems-core-unhandled-category)                                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`protocols-core/contract-graph-diagnostics`](#protocols-core-contract-graph-diagnostics)                           | ValidationError       |    422 | not-retryable | public        |       1 |
| [`protocols-graphql/auth-invalid-header-format`](#protocols-graphql-auth-invalid-header-format)                     | BadRequest            |    400 | not-retryable | public        |       1 |
| [`protocols-graphql/auth-invalid-request`](#protocols-graphql-auth-invalid-request)                                 | BadRequest            |    400 | not-retryable | public        |       1 |
| [`protocols-graphql/auth-invalid-token`](#protocols-graphql-auth-invalid-token)                                     | Unauthorized          |    401 | not-retryable | safe-message  |       1 |
| [`protocols-graphql/auth-missing-header`](#protocols-graphql-auth-missing-header)                                   | Unauthorized          |    401 | not-retryable | safe-message  |       1 |
| [`protocols-graphql/auth-verifier-unavailable`](#protocols-graphql-auth-verifier-unavailable)                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`protocols-graphql/guard-denied`](#protocols-graphql-guard-denied)                                                 | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`protocols-rest/auth-invalid-header-format`](#protocols-rest-auth-invalid-header-format)                           | BadRequest            |    400 | not-retryable | public        |       1 |
| [`protocols-rest/auth-invalid-request`](#protocols-rest-auth-invalid-request)                                       | BadRequest            |    400 | not-retryable | public        |       1 |
| [`protocols-rest/auth-invalid-token`](#protocols-rest-auth-invalid-token)                                           | Unauthorized          |    401 | not-retryable | safe-message  |       1 |
| [`protocols-rest/auth-missing-header`](#protocols-rest-auth-missing-header)                                         | Unauthorized          |    401 | not-retryable | safe-message  |       1 |
| [`protocols-rest/auth-verifier-unavailable`](#protocols-rest-auth-verifier-unavailable)                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`protocols-rest/duplicate-parameter-index`](#protocols-rest-duplicate-parameter-index)                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`protocols-rest/request-validation-failed`](#protocols-rest-request-validation-failed)                             | ValidationError       |    422 | not-retryable | public        |       1 |
| [`protocols-rest/response-validation-failed`](#protocols-rest-response-validation-failed)                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`protocols-rest/validation-failed`](#protocols-rest-validation-failed)                                             | ValidationError       |    422 | not-retryable | public        |       1 |
| [`PUBLIC_EMAIL_DOMAIN_NOT_ALLOWED`](#public-email-domain-not-allowed)                                               | BadRequest            |    400 | not-retryable | public        |       1 |
| [`RATE_LIMIT_EXCEEDED`](#rate-limit-exceeded)                                                                       | TooManyRequests       |    429 | retryable     | safe-message  |       1 |
| [`RATE_LIMIT_KEY_BUILDER_ERROR`](#rate-limit-key-builder-error)                                                     | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`RATE_LIMIT_REFUND_UNSUPPORTED`](#rate-limit-refund-unsupported)                                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`RATE_LIMIT_WINDOW_ERROR`](#rate-limit-window-error)                                                               | BadRequest            |    400 | not-retryable | public        |       1 |
| [`ratelimit-upstash/invalid-policy`](#ratelimit-upstash-invalid-policy)                                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`ratelimit-upstash/missing-config`](#ratelimit-upstash-missing-config)                                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`repository-core/batch-loader-factory-not-registered`](#repository-core-batch-loader-factory-not-registered)       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`repository-core/batch-loader-factory-resolution-failed`](#repository-core-batch-loader-factory-resolution-failed) | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`RESEND_NOTIFICATION_FAILED`](#resend-notification-failed)                                                         | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`RETRY_ABORTED`](#retry-aborted)                                                                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`RETRY_CIRCUIT_BREAKER_INVALID_STATE`](#retry-circuit-breaker-invalid-state)                                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`RETRY_CIRCUIT_BREAKER_LOCK_FAILED`](#retry-circuit-breaker-lock-failed)                                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`RETRY_EXHAUSTED`](#retry-exhausted)                                                                               | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`retry-core/circuit-breaker-unexpected-state`](#retry-core-circuit-breaker-unexpected-state)                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`ROLE_HIERARCHY_VIOLATION`](#role-hierarchy-violation)                                                             | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`rpc-codegen/controller-typescript-diagnostics`](#rpc-codegen-controller-typescript-diagnostics)                   | ValidationError       |    422 | not-retryable | public        |       1 |
| [`rpc-codegen/invalid-contract`](#rpc-codegen-invalid-contract)                                                     | ValidationError       |    422 | not-retryable | public        |       1 |
| [`rpc-codegen/no-rest-controllers-found`](#rpc-codegen-no-rest-controllers-found)                                   | BadRequest            |    400 | not-retryable | public        |       1 |
| [`rpc-codegen/unsupported-form-schema`](#rpc-codegen-unsupported-form-schema)                                       | ValidationError       |    422 | not-retryable | public        |       1 |
| [`saas-demo/demo-endpoint-disabled`](#saas-demo-demo-endpoint-disabled)                                             | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`saas-demo/invalid-jobs-query`](#saas-demo-invalid-jobs-query)                                                     | ValidationError       |    422 | not-retryable | public        |       1 |
| [`saas-demo/invalid-port`](#saas-demo-invalid-port)                                                                 | ValidationError       |    422 | not-retryable | public        |       1 |
| [`saas-demo/job-not-found`](#saas-demo-job-not-found)                                                               | NotFound              |    404 | not-retryable | public        |       1 |
| [`saas-demo/smoke-failed`](#saas-demo-smoke-failed)                                                                 | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`saas-demo/tenant-already-exists`](#saas-demo-tenant-already-exists)                                               | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`saas-demo/tenant-not-found`](#saas-demo-tenant-not-found)                                                         | NotFound              |    404 | not-retryable | public        |       1 |
| [`SEARCH_CAPABILITY_UNAVAILABLE`](#search-capability-unavailable)                                                   | NotImplemented        |    501 | not-retryable | public        |       1 |
| [`SEARCH_DRIZZLE_INVALID_ROW`](#search-drizzle-invalid-row)                                                         | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`search-core/transform-not-found`](#search-core-transform-not-found)                                               | NotFound              |    404 | not-retryable | public        |       1 |
| [`search-meilisearch/tenant-token-not-configured`](#search-meilisearch-tenant-token-not-configured)                 | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`SEAT_LIMIT_EXCEEDED`](#seat-limit-exceeded)                                                                       | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`SELF_IMPERSONATION_NOT_ALLOWED`](#self-impersonation-not-allowed)                                                 | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`starter/invalid-environment`](#starter-invalid-environment)                                                       | ValidationError       |    422 | not-retryable | public        |       1 |
| [`starter/user-not-found`](#starter-user-not-found)                                                                 | NotFound              |    404 | not-retryable | public        |       1 |
| [`STORAGE_DELETE_FAILED`](#storage-delete-failed)                                                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`STORAGE_FILE_NOT_FOUND`](#storage-file-not-found)                                                                 | NotFound              |    404 | not-retryable | public        |       1 |
| [`STORAGE_INVALID_KEY`](#storage-invalid-key)                                                                       | BadRequest            |    400 | not-retryable | public        |       1 |
| [`STORAGE_R2_EMPTY_BODY`](#storage-r2-empty-body)                                                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`STORAGE_R2_MISSING_CONFIG`](#storage-r2-missing-config)                                                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`STORAGE_R2_OBJECT_TOO_LARGE`](#storage-r2-object-too-large)                                                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`STORAGE_UPLOAD_FAILED`](#storage-upload-failed)                                                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`storage-cloudinary/invalid-upload-intent-ttl`](#storage-cloudinary-invalid-upload-intent-ttl)                     | BadRequest            |    400 | not-retryable | public        |       1 |
| [`storage/invalid-upload-intent-ttl`](#storage-invalid-upload-intent-ttl)                                           | BadRequest            |    400 | not-retryable | public        |       1 |
| [`STRATEGY_UNAVAILABLE`](#strategy-unavailable)                                                                     | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`STRUCTURED_OUTPUT_ERROR`](#structured-output-error)                                                               | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`tasks-core/duplicate-task-registration`](#tasks-core-duplicate-task-registration)                                 | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`tasks-core/task-not-found`](#tasks-core-task-not-found)                                                           | NotFound              |    404 | not-retryable | public        |       1 |
| [`tasks-core/task-runner-di-failure`](#tasks-core-task-runner-di-failure)                                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`tasks-qstash/invalid-publish-request`](#tasks-qstash-invalid-publish-request)                                     | BadRequest            |    400 | not-retryable | public        |       1 |
| [`tasks-qstash/missing-config`](#tasks-qstash-missing-config)                                                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`TELEMETRY_RUNTIME_ERROR`](#telemetry-runtime-error)                                                               | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`TELEMETRY_SAMPLER_INVALID_CONFIG`](#telemetry-sampler-invalid-config)                                             | BadRequest            |    400 | not-retryable | public        |       1 |
| [`tenant-core/admin-bypass-reason-required`](#tenant-core-admin-bypass-reason-required)                             | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`tenant-core/cross-tenant-leak`](#tenant-core-cross-tenant-leak)                                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`tenant-core/default-tenant-fallback`](#tenant-core-default-tenant-fallback)                                       | BadRequest            |    400 | not-retryable | public        |       1 |
| [`tenant-core/duplicate-tenant-manager-registration`](#tenant-core-duplicate-tenant-manager-registration)           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`tenant-core/isolation-context-missing`](#tenant-core-isolation-context-missing)                                   | Unauthorized          |    401 | not-retryable | safe-message  |       1 |
| [`tenant-core/tenant-manager-not-registered`](#tenant-core-tenant-manager-not-registered)                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`tenant-core/unsafe-query`](#tenant-core-unsafe-query)                                                             | Forbidden             |    403 | not-retryable | safe-message  |       1 |
| [`tenant/not-found`](#tenant-not-found)                                                                             | NotFound              |    404 | not-retryable | public        |       1 |
| [`tenant/required`](#tenant-required)                                                                               | Unauthorized          |    401 | not-retryable | safe-message  |       1 |
| [`testing/after-commit-hooks-failed`](#testing-after-commit-hooks-failed)                                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`testing/telemetry-provider-already-installed`](#testing-telemetry-provider-already-installed)                     | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`testing/transaction-context-not-active`](#testing-transaction-context-not-active)                                 | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`TOKEN_LIMIT_EXCEEDED`](#token-limit-exceeded)                                                                     | BadRequest            |    400 | not-retryable | public        |       1 |
| [`TOOL_EXECUTION_ERROR`](#tool-execution-error)                                                                     | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`transports-graphql/request-body-aborted`](#transports-graphql-request-body-aborted)                               | BadRequest            |    400 | not-retryable | public        |       1 |
| [`transports-graphql/request-body-too-large`](#transports-graphql-request-body-too-large)                           | BadRequest            |    400 | not-retryable | public        |       1 |
| [`transports-graphql/resolvers-not-configured`](#transports-graphql-resolvers-not-configured)                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`transports-graphql/schema-not-configured`](#transports-graphql-schema-not-configured)                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`transports-graphql/server-not-initialized`](#transports-graphql-server-not-initialized)                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`transports-http/duplicate-health-check`](#transports-http-duplicate-health-check)                                 | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`transports-http/duplicate-route-definition`](#transports-http-duplicate-route-definition)                         | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`transports-http/middleware-next-called-multiple-times`](#transports-http-middleware-next-called-multiple-times)   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`transports-http/pipe-resolution-failed`](#transports-http-pipe-resolution-failed)                                 | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`transports-http/provider-resolution-failed`](#transports-http-provider-resolution-failed)                         | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`transports-http/route-method-not-function`](#transports-http-route-method-not-function)                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`transports-http/runtime-capability-invalid`](#transports-http-runtime-capability-invalid)                         | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`transports-http/security-middleware-validation`](#transports-http-security-middleware-validation)                 | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`transports-http/unsupported-route-method`](#transports-http-unsupported-route-method)                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`triggers-core/duplicate-trigger-metadata`](#triggers-core-duplicate-trigger-metadata)                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`triggers-core/duplicate-trigger-metadata-entry`](#triggers-core-duplicate-trigger-metadata-entry)                 | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`triggers-qstash/duplicate-schedule-id`](#triggers-qstash-duplicate-schedule-id)                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`triggers-qstash/execution-failed`](#triggers-qstash-execution-failed)                                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`triggers-qstash/service-resolution-failed`](#triggers-qstash-service-resolution-failed)                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`tx-core/after-commit-hooks-failed`](#tx-core-after-commit-hooks-failed)                                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`tx-core/decorator-misuse`](#tx-core-decorator-misuse)                                                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`tx-core/duplicate-tx-manager-registration`](#tx-core-duplicate-tx-manager-registration)                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`tx-core/manager-not-registered`](#tx-core-manager-not-registered)                                                 | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`tx-core/missing-transaction-context`](#tx-core-missing-transaction-context)                                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`tx-core/propagation-error`](#tx-core-propagation-error)                                                           | BusinessRuleViolation |    422 | conditional   | safe-message  |       1 |
| [`tx-core/transaction-timeout`](#tx-core-transaction-timeout)                                                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`tx-drizzle/rls-execute-unsupported`](#tx-drizzle-rls-execute-unsupported)                                         | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`tx-drizzle/savepoint-unsupported`](#tx-drizzle-savepoint-unsupported)                                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`tx-drizzle/tenant-context-required`](#tx-drizzle-tenant-context-required)                                         | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`UNAUTHORIZED`](#unauthorized)                                                                                     | Unauthorized          |    401 | not-retryable | safe-message  |       1 |
| [`WEBHOOK_PROCESSING_FAILED`](#webhook-processing-failed)                                                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`WEBHOOK_VALIDATION_FAILED`](#webhook-validation-failed)                                                           | BadRequest            |    400 | not-retryable | public        |       1 |
| [`webhooks-core/configuration`](#webhooks-core-configuration)                                                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`webhooks-core/dispatch-failed`](#webhooks-core-dispatch-failed)                                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`webhooks-core/duplicate-event`](#webhooks-core-duplicate-event)                                                   | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`webhooks-core/invalid-envelope`](#webhooks-core-invalid-envelope)                                                 | BadRequest            |    400 | not-retryable | public        |       1 |
| [`webhooks-core/invalid-fixture`](#webhooks-core-invalid-fixture)                                                   | BadRequest            |    400 | not-retryable | public        |       1 |
| [`webhooks-core/invalid-signature`](#webhooks-core-invalid-signature)                                               | BadRequest            |    400 | not-retryable | public        |       1 |
| [`webhooks-core/reporter-failed`](#webhooks-core-reporter-failed)                                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`webhooks-core/unknown-event`](#webhooks-core-unknown-event)                                                       | BadRequest            |    400 | not-retryable | public        |       1 |
| [`workflow-core/duplicate-workflow-registration`](#workflow-core-duplicate-workflow-registration)                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`workflow-core/replay-unsupported`](#workflow-core-replay-unsupported)                                             | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`workflow-core/saga-definition-invalid`](#workflow-core-saga-definition-invalid)                                   | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`workflow-core/saga-execution-failed`](#workflow-core-saga-execution-failed)                                       | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`workflow-core/saga-execution-not-found`](#workflow-core-saga-execution-not-found)                                 | NotFound              |    404 | not-retryable | public        |       1 |
| [`workflow-core/saga-replay-invalid`](#workflow-core-saga-replay-invalid)                                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`workflow-core/saga-store-conflict`](#workflow-core-saga-store-conflict)                                           | Conflict              |    409 | conditional   | safe-message  |       1 |
| [`workflow-core/workflow-definition-invalid`](#workflow-core-workflow-definition-invalid)                           | InternalServerError   |    500 | conditional   | operator-only |       1 |
| [`workflow-core/workflow-not-found`](#workflow-core-workflow-not-found)                                             | NotFound              |    404 | not-retryable | public        |       1 |

<a id="access-denied"></a>

## `ACCESS_DENIED`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/transports-http/src/libs/PipelineRunner.ts:161:15` (problem-factory)

<a id="access-core-forbidden"></a>

## `access-core/forbidden`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/access-core/src/libs/guards/AccessGuard.ts:17:5` (problem-constructor)

<a id="admin-console-user-not-found"></a>

## `admin-console/user-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/templates/admin-console/apps/api-server/src/controllers/AdminController.ts:45:20` (problem-metadata)

<a id="admin-core-resource-validation-failed"></a>

## `admin-core/resource-validation-failed`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/admin-core/src/libs/AdminResource.ts:79:5` (problem-constructor)

<a id="admin-generated-contract-diagnostics"></a>

## `admin-generated/contract-diagnostics`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/admin-generated/src/libs/generate.ts:55:5` (problem-constructor)

<a id="ai-saas-model-not-found"></a>

## `ai-saas/model-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/templates/ai-saas/apps/api-server/src/aiProblems.ts:31:3` (problem-class)

<a id="ai-saas-model-required"></a>

## `ai-saas/model-required`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/templates/ai-saas/apps/api-server/src/aiProblems.ts:22:3` (problem-class)

<a id="ai-saas-provider-unavailable"></a>

## `ai-saas/provider-unavailable`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/templates/ai-saas/apps/api-server/src/aiProblems.ts:62:3` (problem-class)

<a id="ai-saas-quota-exceeded"></a>

## `ai-saas/quota-exceeded`

- Category: `TooManyRequests`
- HTTP status: `429` Too Many Requests
- Retryability: `retryable`
- Redaction policy: `safe-message`
- Cause: The caller exceeded a rate, quota, or concurrency limit.
- User action: Wait for the retry window or reduce request volume.
- Operator action: Check limiter state, quota configuration, and abuse signals.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/templates/ai-saas/apps/api-server/src/aiProblems.ts:40:3` (problem-class)

<a id="ai-saas-rate-limit-exceeded"></a>

## `ai-saas/rate-limit-exceeded`

- Category: `TooManyRequests`
- HTTP status: `429` Too Many Requests
- Retryability: `retryable`
- Redaction policy: `safe-message`
- Cause: The caller exceeded a rate, quota, or concurrency limit.
- User action: Wait for the retry window or reduce request volume.
- Operator action: Check limiter state, quota configuration, and abuse signals.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/templates/ai-saas/apps/api-server/src/aiProblems.ts:53:3` (problem-class)

<a id="ai-saas-smoke-failed"></a>

## `ai-saas/smoke-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/templates/ai-saas/apps/api-server/src/aiProblems.ts:75:3` (problem-class)

<a id="ai-saas-tenant-not-found"></a>

## `ai-saas/tenant-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/templates/ai-saas/apps/api-server/src/aiProblems.ts:13:3` (problem-class)

<a id="ai-saas-tenant-required"></a>

## `ai-saas/tenant-required`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/templates/ai-saas/apps/api-server/src/aiProblems.ts:4:3` (problem-class)

<a id="already-member"></a>

## `ALREADY_MEMBER`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/membership-core/src/libs/problems/MembershipProblems.ts:18:5` (problem-constructor)

<a id="api-key-expired"></a>

## `API_KEY_EXPIRED`

- Category: `Unauthorized`
- HTTP status: `401` Unauthorized
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The request did not include valid authentication credentials.
- User action: Sign in again or provide a valid credential.
- Operator action: Check authentication configuration, token issuer, and clock skew.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-core/src/libs/problems/AuthProblems.ts:20:3` (problem-class)

<a id="api-key-not-found"></a>

## `API_KEY_NOT_FOUND`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-core/src/libs/apikey/problems/ApiKeyNotFoundProblem.ts:4:3` (problem-class)

<a id="api-key-revoked"></a>

## `API_KEY_REVOKED`

- Category: `Unauthorized`
- HTTP status: `401` Unauthorized
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The request did not include valid authentication credentials.
- User action: Sign in again or provide a valid credential.
- Operator action: Check authentication configuration, token issuer, and clock skew.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-core/src/libs/problems/AuthProblems.ts:28:3` (problem-class)

<a id="architecture-policy-manifest-json-parse"></a>

## `architecture-policy/manifest-json-parse`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/architecture-policy/src/index.ts:19:5` (problem-constructor)

<a id="architecture-policy-manifest-schema-version"></a>

## `architecture-policy/manifest-schema-version`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/architecture-policy/src/index.ts:30:5` (problem-constructor)

<a id="architecture-policy-manifest-shape"></a>

## `architecture-policy/manifest-shape`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/architecture-policy/src/index.ts:9:5` (problem-constructor)

<a id="architecture-policy-package-json-parse"></a>

## `architecture-policy/package-json-parse`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/architecture-policy/src/index.ts:46:5` (problem-constructor)

<a id="audit-core-auditable-decorator-misuse"></a>

## `audit-core/auditable-decorator-misuse`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/audit-core/src/libs/problems/AuditableDecoratorProblem.ts:4:3` (problem-class)

<a id="audit-insert-failed"></a>

## `audit/insert-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/audit-drizzle/src/libs/DrizzleAuditLogRepository.ts:137:13` (problem-factory)

<a id="auth-better-auth-invalid-session-payload"></a>

## `auth-better-auth/invalid-session-payload`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-better-auth/src/libs/problems/BetterAuthInvalidSessionProblem.ts:7:3` (problem-class)

<a id="auth-better-auth-invalid-webhook-payload"></a>

## `auth-better-auth/invalid-webhook-payload`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-better-auth/src/libs/problems/WebhookProblems.ts:23:3` (problem-class)

<a id="auth-better-auth-invalid-webhook-signature"></a>

## `auth-better-auth/invalid-webhook-signature`

- Category: `Unauthorized`
- HTTP status: `401` Unauthorized
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The request did not include valid authentication credentials.
- User action: Sign in again or provide a valid credential.
- Operator action: Check authentication configuration, token issuer, and clock skew.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-better-auth/src/libs/problems/WebhookProblems.ts:7:3` (problem-class)

<a id="auth-better-auth-not-initialized"></a>

## `auth-better-auth/not-initialized`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-better-auth/src/libs/problems/AuthProblems.ts:7:3` (problem-class)

<a id="auth-better-auth-session-lookup-failed"></a>

## `auth-better-auth/session-lookup-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-better-auth/src/libs/problems/BetterAuthSessionLookupProblem.ts:7:3` (problem-class)

<a id="auth-better-auth-session-not-found"></a>

## `auth-better-auth/session-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-better-auth/src/libs/problems/AuthProblems.ts:23:3` (problem-class)

<a id="auth-better-auth-user-not-found"></a>

## `auth-better-auth/user-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-better-auth/src/libs/problems/AuthProblems.ts:39:3` (problem-class)

<a id="auth-clerk-duplicate-tenant-mapping"></a>

## `auth-clerk/duplicate-tenant-mapping`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-clerk/src/libs/problems/ClerkProblems.ts:40:3` (problem-class)

<a id="auth-clerk-external-service-error"></a>

## `auth-clerk/external-service-error`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-clerk/src/libs/problems/ClerkProblems.ts:61:5` (problem-constructor)

<a id="auth-clerk-invalid-webhook-payload"></a>

## `auth-clerk/invalid-webhook-payload`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-clerk/src/libs/problems/ClerkProblems.ts:19:5` (problem-constructor)

<a id="auth-clerk-malformed-claim"></a>

## `auth-clerk/malformed-claim`

- Category: `Unauthorized`
- HTTP status: `401` Unauthorized
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The request did not include valid authentication credentials.
- User action: Sign in again or provide a valid credential.
- Operator action: Check authentication configuration, token issuer, and clock skew.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-clerk/src/libs/problems/ClerkProblems.ts:32:3` (problem-class)

<a id="auth-clerk-public-user-data-missing"></a>

## `auth-clerk/public-user-data-missing`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-clerk/src/libs/problems/ClerkProblems.ts:52:3` (problem-class)

<a id="auth-clerk-token-verification-failed"></a>

## `auth-clerk/token-verification-failed`

- Category: `Unauthorized`
- HTTP status: `401` Unauthorized
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The request did not include valid authentication credentials.
- User action: Sign in again or provide a valid credential.
- Operator action: Check authentication configuration, token issuer, and clock skew.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-clerk/src/libs/problems/ClerkProblems.ts:24:3` (problem-class)

<a id="auth-clerk-webhook-verification-failed"></a>

## `auth-clerk/webhook-verification-failed`

- Category: `Unauthorized`
- HTTP status: `401` Unauthorized
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The request did not include valid authentication credentials.
- User action: Sign in again or provide a valid credential.
- Operator action: Check authentication configuration, token issuer, and clock skew.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-clerk/src/libs/problems/ClerkProblems.ts:5:3` (problem-class)

<a id="auth-core-api-key-creation-failed"></a>

## `auth-core/api-key-creation-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-core/src/libs/problems/AuthProblems.ts:52:3` (problem-class)

<a id="auth-core-invalid-permission-action"></a>

## `auth-core/invalid-permission-action`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-core/src/libs/problems/AuthProblems.ts:44:3` (problem-class)

<a id="auth-core-invalid-permission-format"></a>

## `auth-core/invalid-permission-format`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-core/src/libs/problems/AuthProblems.ts:36:3` (problem-class)

<a id="bad-request"></a>

## `BAD_REQUEST`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/access-core/src/libs/guards/AccessGuard.ts:11:5` (problem-constructor)

<a id="billing-status-mapping-failed"></a>

## `BILLING_STATUS_MAPPING_FAILED`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/billing-polar/src/libs/problems/BillingStatusMappingProblem.ts:4:3` (problem-class)

<a id="billing-polar-customer-not-found"></a>

## `billing-polar/customer-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/billing-polar/src/libs/problems/PolarBillingProblems.ts:51:5` (problem-constructor)

<a id="billing-polar-missing-config"></a>

## `billing-polar/missing-config`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/billing-polar/src/libs/problems/PolarBillingProblems.ts:18:5` (problem-constructor)

<a id="billing-polar-retryable-upstream"></a>

## `billing-polar/retryable-upstream`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/billing-polar/src/libs/problems/PolarBillingProblems.ts:77:5` (problem-constructor)

<a id="billing-polar-subscription-not-found"></a>

## `billing-polar/subscription-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/billing-polar/src/libs/problems/PolarBillingProblems.ts:64:5` (problem-constructor)

<a id="billing-polar-terminal-upstream"></a>

## `billing-polar/terminal-upstream`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/billing-polar/src/libs/problems/PolarBillingProblems.ts:93:5` (problem-constructor)

<a id="billing-polar-validation-failed"></a>

## `billing-polar/validation-failed`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/billing-polar/src/libs/problems/PolarBillingProblems.ts:38:5` (problem-constructor)

<a id="billing-account-not-found"></a>

## `billing/account-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/billing-core/src/libs/problems/BillingProblems.ts:12:3` (problem-class)

<a id="billing-checkout-creation-failed"></a>

## `billing/checkout-creation-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/billing-core/src/libs/problems/BillingProblems.ts:28:3` (problem-class)

<a id="billing-invalid-money-amount"></a>

## `billing/invalid-money-amount`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/billing-core/src/libs/problems/BillingProblems.ts:36:3` (problem-class)

<a id="billing-invalid-money-currency"></a>

## `billing/invalid-money-currency`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/billing-core/src/libs/problems/BillingProblems.ts:44:3` (problem-class)

<a id="billing-money-currency-mismatch"></a>

## `billing/money-currency-mismatch`

- Category: `BusinessRuleViolation`
- HTTP status: `422` Business Rule Violation
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request is syntactically valid but violates a domain rule.
- User action: Change the workflow state or request values so the business rule is satisfied.
- Operator action: Review domain policy, entitlement, quota, and lifecycle rule evidence.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/billing-core/src/libs/problems/BillingProblems.ts:52:3` (problem-class)

<a id="billing-money-division-by-zero"></a>

## `billing/money-division-by-zero`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/billing-core/src/libs/problems/BillingProblems.ts:64:3` (problem-class)

<a id="billing-subscription-not-found"></a>

## `billing/subscription-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/billing-core/src/libs/problems/BillingProblems.ts:4:3` (problem-class)

<a id="billing-webhook-already-processed"></a>

## `billing/webhook-already-processed`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/billing-core/src/libs/problems/BillingProblems.ts:20:3` (problem-class)

<a id="blocked-during-impersonation"></a>

## `BLOCKED_DURING_IMPERSONATION`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/impersonation-core/src/libs/problems/ImpersonationProblems.ts:31:3` (problem-class)

<a id="cache-core-invalid-decorator-config"></a>

## `cache-core/invalid-decorator-config`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/cache-core/src/libs/problems/CacheDecoratorProblems.ts:4:3` (problem-class)

<a id="circuit-breaker-open"></a>

## `CIRCUIT_BREAKER_OPEN`

- Category: `TooManyRequests`
- HTTP status: `429` Too Many Requests
- Retryability: `retryable`
- Redaction policy: `safe-message`
- Cause: The caller exceeded a rate, quota, or concurrency limit.
- User action: Wait for the retry window or reduce request volume.
- Operator action: Check limiter state, quota configuration, and abuse signals.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/retry-core/src/libs/errors/CircuitBreakerOpenProblem.ts:7:3` (problem-class)

<a id="cli-invalid-jobs-number"></a>

## `cli/invalid-jobs-number`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/cli/src/commands/jobs.ts:97:5` (problem-constructor)

<a id="cli-invalid-jobs-target-url"></a>

## `cli/invalid-jobs-target-url`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/cli/src/commands/jobs.ts:87:5` (problem-constructor)

<a id="cli-invalid-ops-target-url"></a>

## `cli/invalid-ops-target-url`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/cli/src/libs/ops.ts:64:5` (problem-constructor)

<a id="cli-invalid-ops-timeout"></a>

## `cli/invalid-ops-timeout`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/cli/src/libs/ops.ts:54:5` (problem-constructor)

<a id="cli-missing-jobs-target-url"></a>

## `cli/missing-jobs-target-url`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/cli/src/commands/jobs.ts:107:5` (problem-constructor)

<a id="cloudflare-images-invalid-ttl"></a>

## `cloudflare/images-invalid-ttl`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/storage-cloudflare/src/libs/CloudflareImagesProvider.ts:44:13` (problem-factory)

<a id="cloudflare-images-null-result"></a>

## `cloudflare/images-null-result`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/storage-cloudflare/src/libs/CloudflareImagesProvider.ts:206:13` (problem-factory)

<a id="cloudflare-images-upload-intent-null-result"></a>

## `cloudflare/images-upload-intent-null-result`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/storage-cloudflare/src/libs/CloudflareImagesProvider.ts:267:13` (problem-factory)

<a id="conflicting-pagination"></a>

## `CONFLICTING_PAGINATION`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/pagination-core/src/libs/problems.ts:17:3` (problem-class)

<a id="create-croco-app-directory-not-empty"></a>

## `create-croco-app/directory-not-empty`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/src/libs/problems/DirectoryNotEmptyProblem.ts:4:3` (problem-class)

<a id="create-croco-app-invalid-cli-option"></a>

## `create-croco-app/invalid-cli-option`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/src/libs/problems/InvalidCliOptionProblem.ts:4:3` (problem-class)

<a id="create-croco-app-invalid-goal-option"></a>

## `create-croco-app/invalid-goal-option`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/src/libs/problems/InvalidGoalOptionProblem.ts:5:3` (problem-class)

<a id="create-croco-app-invalid-saas-preset-option"></a>

## `create-croco-app/invalid-saas-preset-option`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/src/libs/problems/InvalidSaasPresetOptionProblem.ts:4:3` (problem-class)

<a id="create-croco-app-unexpected-failure"></a>

## `create-croco-app/unexpected-failure`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/src/cli-result.ts:25:3` (problem-class)

<a id="dataloader-core-batch-result-length-mismatch"></a>

## `dataloader-core/batch-result-length-mismatch`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/dataloader-core/src/libs/problems/BatchLoaderProblems.ts:4:3` (problem-class)

<a id="diagnostics-core-duplicate-provider"></a>

## `diagnostics-core/duplicate-provider`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/diagnostics-core/src/libs/problems/DiagnosticsProblems.ts:7:3` (problem-class)

<a id="duplicate-invitation"></a>

## `DUPLICATE_INVITATION`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/invitation-core/src/libs/problems/RateLimitProblems.ts:13:3` (problem-class)

<a id="duplicate-recover-handler"></a>

## `DUPLICATE_RECOVER_HANDLER`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/retry-core/src/libs/errors/DuplicateRecoverHandlerProblem.ts:7:3` (problem-class)

<a id="duration-parse-error"></a>

## `DURATION_PARSE_ERROR`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-context/src/libs/problems/ContextProblems.ts:15:3` (problem-class)

<a id="embedding-error"></a>

## `EMBEDDING_ERROR`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-core/src/libs/problems/LlmServiceProblem.ts:43:5` (problem-constructor)

<a id="entitlement-denied"></a>

## `ENTITLEMENT_DENIED`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/entitlements-core/src/libs/problems/EntitlementProblems.ts:19:3` (problem-class)

<a id="entitlement-inactive-subscription"></a>

## `ENTITLEMENT_INACTIVE_SUBSCRIPTION`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/entitlements-core/src/libs/problems/EntitlementProblems.ts:45:3` (problem-class)

<a id="entitlement-missing-plan"></a>

## `ENTITLEMENT_MISSING_PLAN`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/entitlements-core/src/libs/problems/EntitlementProblems.ts:31:3` (problem-class)

<a id="entitlement-not-found"></a>

## `ENTITLEMENT_NOT_FOUND`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/entitlements-core/src/libs/problems/EntitlementProblems.ts:85:3` (problem-class)

<a id="entitlement-provider-unavailable"></a>

## `ENTITLEMENT_PROVIDER_UNAVAILABLE`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/entitlements-core/src/libs/problems/EntitlementProblems.ts:73:3` (problem-class)

<a id="entitlement-quota-exceeded"></a>

## `ENTITLEMENT_QUOTA_EXCEEDED`

- Category: `TooManyRequests`
- HTTP status: `429` Too Many Requests
- Retryability: `retryable`
- Redaction policy: `safe-message`
- Cause: The caller exceeded a rate, quota, or concurrency limit.
- User action: Wait for the retry window or reduce request volume.
- Operator action: Check limiter state, quota configuration, and abuse signals.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/entitlements-core/src/libs/problems/EntitlementProblems.ts:59:3` (problem-class)

<a id="entitlement-requirement-invalid"></a>

## `ENTITLEMENT_REQUIREMENT_INVALID`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/entitlements-core/src/libs/problems/EntitlementProblems.ts:10:3` (problem-class)

<a id="events-core-after-commit-requires-active-transaction"></a>

## `events-core/after-commit-requires-active-transaction`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/events-core/src/libs/problems/EventsProblems.ts:96:3` (problem-class)

<a id="events-core-deserialization-error"></a>

## `events-core/deserialization-error`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/events-core/src/libs/problems/EventsProblems.ts:40:3` (problem-class)

<a id="events-core-duplicate-event-field"></a>

## `events-core/duplicate-event-field`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/events-core/src/libs/problems/EventsProblems.ts:51:3` (problem-class)

<a id="events-core-duplicate-event-name"></a>

## `events-core/duplicate-event-name`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/events-core/src/libs/problems/EventsProblems.ts:66:3` (problem-class)

<a id="events-core-event-bus-not-set"></a>

## `events-core/event-bus-not-set`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/events-core/src/libs/problems/EventsProblems.ts:7:3` (problem-class)

<a id="events-core-event-definition-error"></a>

## `events-core/event-definition-error`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/events-core/src/libs/problems/EventsProblems.ts:18:3` (problem-class)

<a id="events-core-transaction-context-unavailable"></a>

## `events-core/transaction-context-unavailable`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/events-core/src/libs/problems/EventsProblems.ts:81:3` (problem-class)

<a id="events-core-unknown-event-type"></a>

## `events-core/unknown-event-type`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/events-core/src/libs/problems/EventsProblems.ts:29:3` (problem-class)

<a id="events-inmemory-backpressure-exceeded"></a>

## `events-inmemory/backpressure-exceeded`

- Category: `TooManyRequests`
- HTTP status: `429` Too Many Requests
- Retryability: `retryable`
- Redaction policy: `safe-message`
- Cause: The caller exceeded a rate, quota, or concurrency limit.
- User action: Wait for the retry window or reduce request volume.
- Operator action: Check limiter state, quota configuration, and abuse signals.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/events-inmemory/src/libs/problems/EventsInmemoryProblems.ts:7:3` (problem-class)

<a id="events-inmemory-backpressure-timeout"></a>

## `events-inmemory/backpressure-timeout`

- Category: `TooManyRequests`
- HTTP status: `429` Too Many Requests
- Retryability: `retryable`
- Redaction policy: `safe-message`
- Cause: The caller exceeded a rate, quota, or concurrency limit.
- User action: Wait for the retry window or reduce request volume.
- Operator action: Check limiter state, quota configuration, and abuse signals.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/events-inmemory/src/libs/problems/EventsInmemoryProblems.ts:23:3` (problem-class)

<a id="events-inmemory-invalid-configuration"></a>

## `events-inmemory/invalid-configuration`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/events-inmemory/src/libs/InmemoryEventBus.ts:51:3` (problem-class)

<a id="events-inmemory-publish-failed"></a>

## `events-inmemory/publish-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/events-inmemory/src/libs/InmemoryEventBus.ts:34:3` (problem-class)

<a id="events-tx-outbox-publish-exhausted"></a>

## `events-tx/outbox-publish-exhausted`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/events-tx/src/libs/problems/EventsTxProblems.ts:30:3` (problem-class)

<a id="events-tx-outbox-transaction-required"></a>

## `events-tx/outbox-transaction-required`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/events-tx/src/libs/problems/EventsTxProblems.ts:12:3` (problem-class)

<a id="events-tx-storage-error"></a>

## `events-tx/storage-error`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/events-tx/src/libs/problems/EventsTxProblems.ts:21:3` (problem-class)

<a id="events-tx-transaction-state-error"></a>

## `events-tx/transaction-state-error`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/events-tx/src/libs/problems/EventsTxProblems.ts:4:3` (problem-class)

<a id="execution-conflict"></a>

## `execution/conflict`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/execution-core/src/libs/ExecutionProblem.ts:29:12` (problem-constructor)

<a id="execution-invalid-state-transition"></a>

## `execution/invalid-state-transition`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/execution-core/src/libs/ExecutionProblem.ts:41:12` (problem-constructor)

<a id="execution-max-retries-exceeded"></a>

## `execution/max-retries-exceeded`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/execution-core/src/libs/ExecutionProblem.ts:33:12` (problem-constructor)

<a id="execution-not-found"></a>

## `execution/not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/execution-core/src/libs/ExecutionProblem.ts:25:12` (problem-constructor)

<a id="forbidden"></a>

## `FORBIDDEN`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-core/src/libs/problems/AuthProblems.ts:12:3` (problem-class)

<a id="framework-config-config-schema-not-found"></a>

## `framework-config/config-schema-not-found`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-config/src/libs/problems/ConfigProblems.ts:4:3` (problem-class)

<a id="framework-config-config-validation-failed"></a>

## `framework-config/config-validation-failed`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-config/src/libs/problems/ConfigProblems.ts:15:5` (problem-constructor)

<a id="framework-config-invalid-boolean-env"></a>

## `framework-config/invalid-boolean-env`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-config/src/libs/problems/ConfigProblems.ts:24:3` (problem-class)

<a id="framework-context-circular-dependency"></a>

## `framework-context/circular-dependency`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-context/src/libs/problems/CircularDependencyProblem.ts:7:3` (problem-class)

<a id="framework-context-context-middleware-execution-error"></a>

## `framework-context/context-middleware-execution-error`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-context/src/libs/problems/ContextProblems.ts:7:3` (problem-class)

<a id="framework-context-di-resolution-failed"></a>

## `framework-context/di-resolution-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-context/src/libs/problems/ContainerResolutionProblem.ts:10:3` (problem-class)

<a id="framework-context-di-scope-mismatch"></a>

## `framework-context/di-scope-mismatch`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-context/src/libs/problems/ContainerResolutionProblem.ts:31:3` (problem-class)

<a id="framework-context-pipeline-graph-invalid"></a>

## `framework-context/pipeline-graph-invalid`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-context/src/libs/problems/PipelineGraphProblems.ts:7:3` (problem-class)

<a id="framework-context-policy-capability-unavailable"></a>

## `framework-context/policy-capability-unavailable`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-context/src/libs/problems/RuntimePolicyProblems.ts:33:3` (problem-class)

<a id="framework-context-policy-conflict"></a>

## `framework-context/policy-conflict`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-context/src/libs/problems/RuntimePolicyProblems.ts:9:3` (problem-class)

<a id="framework-context-policy-definition-invalid"></a>

## `framework-context/policy-definition-invalid`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-context/src/libs/problems/RuntimePolicyProblems.ts:21:3` (problem-class)

<a id="framework-context-request-scope-outside-context"></a>

## `framework-context/request-scope-outside-context`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-context/src/libs/Container.ts:1147:13` (problem-factory)

<a id="framework-context-shutdown-configuration-conflict"></a>

## `framework-context/shutdown-configuration-conflict`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-context/src/libs/problems/ShutdownProblems.ts:18:3` (problem-class)

<a id="framework-context-shutdown-timeout"></a>

## `framework-context/shutdown-timeout`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-context/src/libs/problems/ShutdownProblems.ts:7:3` (problem-class)

<a id="framework-module-circular-dependency"></a>

## `framework-module/circular-dependency`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-module/src/problems.ts:19:5` (problem-constructor)

<a id="framework-module-invalid-module-definition"></a>

## `framework-module/invalid-module-definition`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-module/src/problems.ts:9:5` (problem-constructor)

<a id="framework-module-lifecycle-failed"></a>

## `framework-module/lifecycle-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-module/src/problems.ts:35:5` (problem-constructor)

<a id="framework-module-provider-not-visible"></a>

## `framework-module/provider-not-visible`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-module/src/problems.ts:51:5` (problem-constructor)

<a id="frontend-vite-missing-cloudflare-vite-plugin"></a>

## `frontend-vite/missing-cloudflare-vite-plugin`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/frontend-vite/src/libs/problems/MissingCloudflareVitePluginProblem.ts:10:5` (problem-constructor)

<a id="generation-error"></a>

## `GENERATION_ERROR`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-core/src/libs/problems/LlmServiceProblem.ts:34:5` (problem-constructor)

<a id="gid-core-id-type-only-property"></a>

## `gid-core/id-type-only-property`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/gid-core/src/libs/problems/GidProblems.ts:17:3` (problem-class)

<a id="gid-core-invalid-id-prefix"></a>

## `gid-core/invalid-id-prefix`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/gid-core/src/libs/problems/GidProblems.ts:4:3` (problem-class)

<a id="governance-core-delete-not-supported"></a>

## `governance-core/delete-not-supported`

- Category: `NotImplemented`
- HTTP status: `501` Not Implemented
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested capability is not supported by this runtime or adapter.
- User action: Use a supported capability or choose an adapter/runtime that provides it.
- Operator action: Check runtime capability declarations and provider maturity documentation.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/governance-core/src/libs/problems/DataGovernanceProblems.ts:34:5` (problem-constructor)

<a id="governance-core-export-not-supported"></a>

## `governance-core/export-not-supported`

- Category: `NotImplemented`
- HTTP status: `501` Not Implemented
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested capability is not supported by this runtime or adapter.
- User action: Use a supported capability or choose an adapter/runtime that provides it.
- Operator action: Check runtime capability declarations and provider maturity documentation.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/governance-core/src/libs/problems/DataGovernanceProblems.ts:15:5` (problem-constructor)

<a id="governance-core-resource-validation-failed"></a>

## `governance-core/resource-validation-failed`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/governance-core/src/libs/DataGovernanceResource.ts:102:5` (problem-constructor)

<a id="governance-core-retention-policy-violation"></a>

## `governance-core/retention-policy-violation`

- Category: `BusinessRuleViolation`
- HTTP status: `422` Business Rule Violation
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request is syntactically valid but violates a domain rule.
- User action: Change the workflow state or request values so the business rule is satisfied.
- Operator action: Review domain policy, entitlement, quota, and lifecycle rule evidence.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/governance-core/src/libs/problems/DataGovernanceProblems.ts:59:5` (problem-constructor)

<a id="graphql-not-found"></a>

## `GRAPHQL_NOT_FOUND`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/protocols-graphql/src/libs/errors/GraphQLProblems.ts:23:5` (problem-constructor)

<a id="health-score-not-found"></a>

## `HEALTH_SCORE_NOT_FOUND`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/customer-health-core/src/libs/problems/HealthProblems.ts:4:3` (problem-class)

<a id="idempotency-core-invalid-key"></a>

## `idempotency-core/invalid-key`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/idempotency-core/src/libs/problems/IdempotencyProblems.ts:52:11` (problem-metadata)

<a id="idempotency-core-key-conflict"></a>

## `idempotency-core/key-conflict`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/idempotency-core/src/libs/problems/IdempotencyProblems.ts:36:11` (problem-metadata)

<a id="idempotency-core-reservation-not-found"></a>

## `idempotency-core/reservation-not-found`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/idempotency-core/src/libs/problems/IdempotencyProblems.ts:63:11` (problem-metadata)

<a id="idempotency-core-reservation-state"></a>

## `idempotency-core/reservation-state`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/idempotency-core/src/libs/problems/IdempotencyProblems.ts:82:11` (problem-metadata)

<a id="impersonation-reason-required"></a>

## `IMPERSONATION_REASON_REQUIRED`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/impersonation-core/src/libs/problems/ImpersonationProblems.ts:22:3` (problem-class)

<a id="impersonation-session-not-found"></a>

## `IMPERSONATION_SESSION_NOT_FOUND`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/impersonation-core/src/libs/problems/ImpersonationProblems.ts:40:3` (problem-class)

<a id="index-not-found"></a>

## `INDEX_NOT_FOUND`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/search-core/src/libs/problems/SearchProblems.ts:39:5` (problem-constructor)

<a id="integrations-posthog-missing-config"></a>

## `integrations-posthog/missing-config`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/integrations-posthog/src/libs/problems/PostHogProblems.ts:4:3` (problem-class)

<a id="invalid-auto-join-role"></a>

## `INVALID_AUTO_JOIN_ROLE`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/invitation-core/src/libs/problems/DomainPolicyProblems.ts:18:5` (problem-constructor)

<a id="invalid-cursor"></a>

## `INVALID_CURSOR`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/pagination-core/src/libs/problems.ts:4:3` (problem-class)

<a id="invalid-retry-configuration"></a>

## `INVALID_RETRY_CONFIGURATION`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/retry-core/src/libs/errors/RetryInfrastructureProblem.ts:46:3` (problem-class)

<a id="invalid-role"></a>

## `INVALID_ROLE`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/membership-core/src/libs/problems/MembershipProblems.ts:44:5` (problem-constructor)

<a id="invitation-already-accepted"></a>

## `INVITATION_ALREADY_ACCEPTED`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/invitation-core/src/libs/problems/InvitationProblems.ts:26:3` (problem-class)

<a id="invitation-email-mismatch"></a>

## `INVITATION_EMAIL_MISMATCH`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/invitation-core/src/libs/problems/InvitationProblems.ts:37:3` (problem-class)

<a id="invitation-expired"></a>

## `INVITATION_EXPIRED`

- Category: `Gone`
- HTTP status: `410` Gone
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource is no longer available through this API surface.
- User action: Stop using the stale reference and follow the replacement flow when available.
- Operator action: Verify lifecycle, migration, deprecation, and retention state.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/invitation-core/src/libs/problems/InvitationProblems.ts:15:3` (problem-class)

<a id="invitation-invalid-status"></a>

## `INVITATION_INVALID_STATUS`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/invitation-core/src/libs/problems/InvitationProblems.ts:48:3` (problem-class)

<a id="invitation-not-found"></a>

## `INVITATION_NOT_FOUND`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/invitation-core/src/libs/problems/InvitationProblems.ts:4:3` (problem-class)

<a id="invitation-rate-limit-exceeded"></a>

## `INVITATION_RATE_LIMIT_EXCEEDED`

- Category: `TooManyRequests`
- HTTP status: `429` Too Many Requests
- Retryability: `retryable`
- Redaction policy: `safe-message`
- Cause: The caller exceeded a rate, quota, or concurrency limit.
- User action: Wait for the retry window or reduce request volume.
- Operator action: Check limiter state, quota configuration, and abuse signals.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/invitation-core/src/libs/problems/RateLimitProblems.ts:4:3` (problem-class)

<a id="invitation-core-batch-size-exceeded"></a>

## `invitation-core/batch-size-exceeded`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/invitation-core/src/libs/problems/BatchInviteProblems.ts:4:3` (problem-class)

<a id="lambda-timeout-guard"></a>

## `LAMBDA_TIMEOUT_GUARD`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/retry-core/src/libs/errors/RetryInfrastructureProblem.ts:33:3` (problem-class)

<a id="last-owner"></a>

## `LAST_OWNER`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/membership-core/src/libs/problems/MembershipProblems.ts:31:5` (problem-constructor)

<a id="lifecycle-core-action-adapter-failed"></a>

## `lifecycle-core/action-adapter-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/lifecycle-core/src/libs/problems/LifecycleProblems.ts:37:5` (problem-constructor)

<a id="lifecycle-core-duplicate-rule"></a>

## `lifecycle-core/duplicate-rule`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/lifecycle-core/src/libs/problems/LifecycleProblems.ts:5:5` (problem-constructor)

<a id="lifecycle-core-rule-definition-invalid"></a>

## `lifecycle-core/rule-definition-invalid`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/lifecycle-core/src/libs/problems/LifecycleProblems.ts:21:5` (problem-constructor)

<a id="llm-provider-not-found"></a>

## `LLM_PROVIDER_NOT_FOUND`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-core/src/libs/problems/LlmProblems.ts:9:5` (problem-constructor)

<a id="llm-service-error"></a>

## `LLM_SERVICE_ERROR`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-core/src/libs/problems/LlmServiceProblem.ts:8:5` (problem-constructor)

<a id="llm-core-invalid-llm-prompt"></a>

## `llm-core/invalid-llm-prompt`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-core/src/libs/problems/LlmProblems.ts:85:5` (problem-constructor)

<a id="llm-core-invalid-llm-response"></a>

## `llm-core/invalid-llm-response`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-core/src/libs/problems/LlmProblems.ts:65:5` (problem-constructor)

<a id="llm-core-llm-service-not-initialized"></a>

## `llm-core/llm-service-not-initialized`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-core/src/libs/problems/LlmProblems.ts:75:5` (problem-constructor)

<a id="llm-core-rate-limit-exceeded"></a>

## `llm-core/rate-limit-exceeded`

- Category: `TooManyRequests`
- HTTP status: `429` Too Many Requests
- Retryability: `retryable`
- Redaction policy: `safe-message`
- Cause: The caller exceeded a rate, quota, or concurrency limit.
- User action: Wait for the retry window or reduce request volume.
- Operator action: Check limiter state, quota configuration, and abuse signals.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-core/src/libs/problems/LlmProblems.ts:43:5` (problem-constructor)

<a id="llm-metering-cost-limit-exceeded"></a>

## `llm-metering/cost-limit-exceeded`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-metering/src/libs/problems/LlmMeteringProblems.ts:41:5` (problem-constructor)

<a id="llm-metering-pricing-not-found"></a>

## `llm-metering/pricing-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-metering/src/libs/problems/LlmMeteringProblems.ts:58:3` (problem-class)

<a id="llm-metering-pricing-registry-conflict"></a>

## `llm-metering/pricing-registry-conflict`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-metering/src/libs/problems/LlmMeteringProblems.ts:71:5` (problem-constructor)

<a id="llm-metering-quota-exceeded"></a>

## `llm-metering/quota-exceeded`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-metering/src/libs/problems/LlmMeteringProblems.ts:24:5` (problem-constructor)

<a id="llm-metering-record-failed"></a>

## `llm-metering/record-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-metering/src/libs/problems/LlmMeteringProblems.ts:7:5` (problem-constructor)

<a id="llm-openai-aborted"></a>

## `llm-openai/aborted`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-openai/src/libs/problems/OpenAiProblems.ts:117:5` (problem-constructor)

<a id="llm-openai-authentication-failed"></a>

## `llm-openai/authentication-failed`

- Category: `Unauthorized`
- HTTP status: `401` Unauthorized
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The request did not include valid authentication credentials.
- User action: Sign in again or provide a valid credential.
- Operator action: Check authentication configuration, token issuer, and clock skew.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-openai/src/libs/problems/OpenAiProblems.ts:42:5` (problem-constructor)

<a id="llm-openai-invalid-response"></a>

## `llm-openai/invalid-response`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-openai/src/libs/problems/OpenAiProblems.ts:135:5` (problem-constructor)

<a id="llm-openai-missing-config"></a>

## `llm-openai/missing-config`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-openai/src/libs/problems/OpenAiProblems.ts:24:5` (problem-constructor)

<a id="llm-openai-rate-limited"></a>

## `llm-openai/rate-limited`

- Category: `TooManyRequests`
- HTTP status: `429` Too Many Requests
- Retryability: `retryable`
- Redaction policy: `safe-message`
- Cause: The caller exceeded a rate, quota, or concurrency limit.
- User action: Wait for the retry window or reduce request volume.
- Operator action: Check limiter state, quota configuration, and abuse signals.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-openai/src/libs/problems/OpenAiProblems.ts:57:5` (problem-constructor)

<a id="llm-openai-retryable-upstream"></a>

## `llm-openai/retryable-upstream`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-openai/src/libs/problems/OpenAiProblems.ts:72:5` (problem-constructor)

<a id="llm-openai-terminal-upstream"></a>

## `llm-openai/terminal-upstream`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-openai/src/libs/problems/OpenAiProblems.ts:87:5` (problem-constructor)

<a id="llm-openai-validation-failed"></a>

## `llm-openai/validation-failed`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-openai/src/libs/problems/OpenAiProblems.ts:102:5` (problem-constructor)

<a id="membership-constraint"></a>

## `MEMBERSHIP_CONSTRAINT`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/membership-core/src/libs/problems/MembershipConstraintProblem.ts:9:5` (problem-constructor)

<a id="membership-not-found"></a>

## `MEMBERSHIP_NOT_FOUND`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/membership-core/src/libs/problems/MembershipProblems.ts:5:5` (problem-constructor)

<a id="meta-vite-server-action-invalid-path"></a>

## `meta-vite/server-action-invalid-path`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/meta-vite/src/libs/actions/serverActions.ts:64:3` (problem-class)

<a id="meta-vite-server-action-not-found"></a>

## `meta-vite/server-action-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/meta-vite/src/libs/actions/serverActions.ts:50:3` (problem-class)

<a id="meta-vite-server-action-validation-failed"></a>

## `meta-vite/server-action-validation-failed`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/meta-vite/src/libs/actions/serverActions.ts:78:3` (problem-class)

<a id="meter-insert-failed"></a>

## `meter/insert-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/metering-drizzle/src/libs/DrizzleMeterRepository.ts:131:13` (problem-factory)

<a id="metering-atomic-quota-not-supported"></a>

## `metering/atomic-quota-not-supported`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/metering-core/src/libs/problems/AtomicQuotaNotSupportedProblem.ts:4:3` (problem-class)

<a id="metering-duplicate-record"></a>

## `metering/duplicate-record`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/metering-core/src/libs/problems/DuplicateRecordProblem.ts:5:5` (problem-constructor)

<a id="metering-invalid-meter"></a>

## `metering/invalid-meter`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/metering-core/src/libs/problems/InvalidMeterProblem.ts:5:5` (problem-constructor)

<a id="metering-quota-exceeded"></a>

## `metering/quota-exceeded`

- Category: `TooManyRequests`
- HTTP status: `429` Too Many Requests
- Retryability: `retryable`
- Redaction policy: `safe-message`
- Cause: The caller exceeded a rate, quota, or concurrency limit.
- User action: Wait for the retry window or reduce request volume.
- Operator action: Check limiter state, quota configuration, and abuse signals.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/metering-core/src/libs/problems/QuotaExceededProblem.ts:9:5` (problem-constructor)

<a id="metering-redis-error"></a>

## `metering/redis-error`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/metering-core/src/libs/problems/RedisProblem.ts:5:5` (problem-constructor)

<a id="metrics-core-carrying-capacity-simulation-error"></a>

## `metrics-core/carrying-capacity-simulation-error`

- Category: `BusinessRuleViolation`
- HTTP status: `422` Business Rule Violation
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request is syntactically valid but violates a domain rule.
- User action: Change the workflow state or request values so the business rule is satisfied.
- Operator action: Review domain policy, entitlement, quota, and lifecycle rule evidence.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/metrics-core/src/libs/problems/MetricsProblems.ts:4:3` (problem-class)

<a id="metrics-core-carrying-capacity-tenant-required"></a>

## `metrics-core/carrying-capacity-tenant-required`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/metrics-core/src/libs/problems/MetricsProblems.ts:12:3` (problem-class)

<a id="metrics-core-gross-margin-required"></a>

## `metrics-core/gross-margin-required`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/metrics-core/src/libs/problems/MetricsProblems.ts:30:3` (problem-class)

<a id="metrics-core-mixed-currency-mrr"></a>

## `metrics-core/mixed-currency-mrr`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/metrics-core/src/libs/problems/MetricsProblems.ts:38:3` (problem-class)

<a id="metrics-core-retention-metrics-unavailable"></a>

## `metrics-core/retention-metrics-unavailable`

- Category: `NotImplemented`
- HTTP status: `501` Not Implemented
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested capability is not supported by this runtime or adapter.
- User action: Use a supported capability or choose an adapter/runtime that provides it.
- Operator action: Check runtime capability declarations and provider maturity documentation.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/metrics-core/src/libs/problems/MetricsProblems.ts:20:3` (problem-class)

<a id="metrics-core-snapshot-tenant-required"></a>

## `metrics-core/snapshot-tenant-required`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/metrics-core/src/libs/SnapshotScheduler.ts:19:3` (problem-class)

<a id="middleware-execution-error"></a>

## `MIDDLEWARE_EXECUTION_ERROR`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/framework-context/src/libs/problems/MiddlewareProblems.ts:7:3` (problem-class)

<a id="migration-runner-database-url-required"></a>

## `migration-runner/database-url-required`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/migration-runner/src/libs/problems/DatabaseUrlRequiredProblem.ts:4:3` (problem-class)

<a id="migration-runner-invalid-count"></a>

## `migration-runner/invalid-count`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/migration-runner/src/libs/problems/InvalidMigrationCountProblem.ts:4:3` (problem-class)

<a id="migration-runner-missing-down-function"></a>

## `migration-runner/missing-down-function`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/migration-runner/src/libs/problems/MissingDownFunctionProblem.ts:4:3` (problem-class)

<a id="migration-runner-missing-up-function"></a>

## `migration-runner/missing-up-function`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/migration-runner/src/libs/problems/MissingUpFunctionProblem.ts:4:3` (problem-class)

<a id="migration-runner-transaction-required"></a>

## `migration-runner/transaction-required`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/migration-runner/src/libs/problems/MigrationTransactionRequiredProblem.ts:4:3` (problem-class)

<a id="migration-runner-unsupported-dialect"></a>

## `migration-runner/unsupported-dialect`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/migration-runner/src/libs/problems/UnsupportedDialectProblem.ts:4:3` (problem-class)

<a id="migration-runner-unsupported-query-result"></a>

## `migration-runner/unsupported-query-result`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/migration-runner/src/libs/problems/UnsupportedMigrationQueryResultProblem.ts:4:3` (problem-class)

<a id="missing-tenant"></a>

## `MISSING_TENANT`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/search-core/src/libs/problems/SearchProblems.ts:5:5` (problem-constructor)

<a id="model-not-found"></a>

## `MODEL_NOT_FOUND`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-core/src/libs/problems/LlmServiceProblem.ts:25:5` (problem-constructor)

<a id="nested-impersonation-not-allowed"></a>

## `NESTED_IMPERSONATION_NOT_ALLOWED`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/impersonation-core/src/libs/problems/ImpersonationProblems.ts:13:3` (problem-class)

<a id="notifications-core-default-provider-conflict"></a>

## `notifications-core/default-provider-conflict`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/notifications-core/src/libs/problems/NotificationProblems.ts:54:5` (problem-constructor)

<a id="notifications-core-delivery-failed"></a>

## `notifications-core/delivery-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/notifications-core/src/libs/problems/NotificationProblems.ts:126:5` (problem-constructor)

<a id="notifications-core-provider-already-registered"></a>

## `notifications-core/provider-already-registered`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/notifications-core/src/libs/problems/NotificationProblems.ts:38:5` (problem-constructor)

<a id="notifications-core-provider-channel-mismatch"></a>

## `notifications-core/provider-channel-mismatch`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/notifications-core/src/libs/problems/NotificationProblems.ts:76:5` (problem-constructor)

<a id="notifications-core-provider-not-configured"></a>

## `notifications-core/provider-not-configured`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/notifications-core/src/libs/problems/NotificationProblems.ts:6:5` (problem-constructor)

<a id="notifications-core-provider-not-found"></a>

## `notifications-core/provider-not-found`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/notifications-core/src/libs/problems/NotificationProblems.ts:94:5` (problem-constructor)

<a id="notifications-core-provider-not-registered"></a>

## `notifications-core/provider-not-registered`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/notifications-core/src/libs/problems/NotificationProblems.ts:22:5` (problem-constructor)

<a id="notifications-core-send-max-attempts-invalid"></a>

## `notifications-core/send-max-attempts-invalid`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/notifications-core/src/libs/problems/NotificationProblems.ts:110:5` (problem-constructor)

<a id="onboarding-context-required"></a>

## `onboarding/context-required`

- Category: `Unauthorized`
- HTTP status: `401` Unauthorized
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The request did not include valid authentication credentials.
- User action: Sign in again or provide a valid credential.
- Operator action: Check authentication configuration, token issuer, and clock skew.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/onboarding-core/src/libs/problems/OnboardingProblems.ts:20:3` (problem-class)

<a id="onboarding-definition-not-found"></a>

## `onboarding/definition-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/onboarding-core/src/libs/problems/OnboardingProblems.ts:4:3` (problem-class)

<a id="onboarding-step-not-found"></a>

## `onboarding/step-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/onboarding-core/src/libs/problems/OnboardingProblems.ts:12:3` (problem-class)

<a id="openapi-spec-controller-typescript-diagnostics"></a>

## `openapi-spec/controller-typescript-diagnostics`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/openapi-spec/src/libs/loadControllers.ts:35:5` (problem-constructor)

<a id="openapi-spec-invalid-contract"></a>

## `openapi-spec/invalid-contract`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/openapi-spec/src/libs/emitOpenAPI.ts:92:5` (problem-constructor)

<a id="openapi-spec-no-rest-controllers-found"></a>

## `openapi-spec/no-rest-controllers-found`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/openapi-spec/src/libs/loadControllers.ts:14:5` (problem-constructor)

<a id="otlp-endpoint-required"></a>

## `OTLP_ENDPOINT_REQUIRED`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/telemetry-sdk-node/src/libs/problems/TelemetryProblems.ts:19:3` (problem-class)

<a id="ownership-transfer-required"></a>

## `OWNERSHIP_TRANSFER_REQUIRED`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/membership-core/src/libs/problems/MembershipProblems.ts:65:5` (problem-constructor)

<a id="problems-core-invalid-extensions"></a>

## `problems-core/invalid-extensions`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/problems-core/src/libs/problems/InvalidExtensionsProblem.ts:5:3` (problem-class)

<a id="problems-core-parse-error"></a>

## `problems-core/parse-error`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/problems-core/src/libs/problems/ProblemDetailsParseProblem.ts:5:3` (problem-class)

<a id="problems-core-problem-registry-invalid"></a>

## `problems-core/problem-registry-invalid`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/problems-core/src/libs/ProblemRegistry.ts:67:5` (problem-constructor)

<a id="problems-core-unhandled-category"></a>

## `problems-core/unhandled-category`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/problems-core/src/libs/ProblemCategoryMapper.ts:10:5` (problem-class)

<a id="protocols-core-contract-graph-diagnostics"></a>

## `protocols-core/contract-graph-diagnostics`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/protocols-core/src/libs/ContractGraph.ts:121:5` (problem-constructor)

<a id="protocols-graphql-auth-invalid-header-format"></a>

## `protocols-graphql/auth-invalid-header-format`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/protocols-graphql/src/libs/guards/AuthGuard.ts:71:13` (problem-factory)

<a id="protocols-graphql-auth-invalid-request"></a>

## `protocols-graphql/auth-invalid-request`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/protocols-graphql/src/libs/guards/AuthGuard.ts:54:13` (problem-factory)

<a id="protocols-graphql-auth-invalid-token"></a>

## `protocols-graphql/auth-invalid-token`

- Category: `Unauthorized`
- HTTP status: `401` Unauthorized
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The request did not include valid authentication credentials.
- User action: Sign in again or provide a valid credential.
- Operator action: Check authentication configuration, token issuer, and clock skew.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/protocols-graphql/src/libs/guards/AuthGuard.ts:14:10` (problem-factory)

<a id="protocols-graphql-auth-missing-header"></a>

## `protocols-graphql/auth-missing-header`

- Category: `Unauthorized`
- HTTP status: `401` Unauthorized
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The request did not include valid authentication credentials.
- User action: Sign in again or provide a valid credential.
- Operator action: Check authentication configuration, token issuer, and clock skew.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/protocols-graphql/src/libs/guards/AuthGuard.ts:63:13` (problem-factory)

<a id="protocols-graphql-auth-verifier-unavailable"></a>

## `protocols-graphql/auth-verifier-unavailable`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/protocols-graphql/src/libs/guards/AuthGuard.ts:97:13` (problem-factory)

<a id="protocols-graphql-guard-denied"></a>

## `protocols-graphql/guard-denied`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/protocols-graphql/src/libs/problems/GuardProblems.ts:4:3` (problem-class)

<a id="protocols-rest-auth-invalid-header-format"></a>

## `protocols-rest/auth-invalid-header-format`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/protocols-rest/src/libs/guards/AuthGuard.ts:77:13` (problem-factory)

<a id="protocols-rest-auth-invalid-request"></a>

## `protocols-rest/auth-invalid-request`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/protocols-rest/src/libs/guards/AuthGuard.ts:59:13` (problem-factory)

<a id="protocols-rest-auth-invalid-token"></a>

## `protocols-rest/auth-invalid-token`

- Category: `Unauthorized`
- HTTP status: `401` Unauthorized
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The request did not include valid authentication credentials.
- User action: Sign in again or provide a valid credential.
- Operator action: Check authentication configuration, token issuer, and clock skew.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/protocols-rest/src/libs/guards/AuthGuard.ts:15:10` (problem-factory)

<a id="protocols-rest-auth-missing-header"></a>

## `protocols-rest/auth-missing-header`

- Category: `Unauthorized`
- HTTP status: `401` Unauthorized
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The request did not include valid authentication credentials.
- User action: Sign in again or provide a valid credential.
- Operator action: Check authentication configuration, token issuer, and clock skew.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/protocols-rest/src/libs/guards/AuthGuard.ts:69:13` (problem-factory)

<a id="protocols-rest-auth-verifier-unavailable"></a>

## `protocols-rest/auth-verifier-unavailable`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/protocols-rest/src/libs/guards/AuthGuard.ts:102:13` (problem-factory)

<a id="protocols-rest-duplicate-parameter-index"></a>

## `protocols-rest/duplicate-parameter-index`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/protocols-rest/src/libs/metadata/MetadataReader.ts:48:13` (problem-factory)

<a id="protocols-rest-request-validation-failed"></a>

## `protocols-rest/request-validation-failed`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/protocols-rest/src/libs/validators/ValidationProblem.ts:31:3` (problem-class)

<a id="protocols-rest-response-validation-failed"></a>

## `protocols-rest/response-validation-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/protocols-rest/src/libs/validators/ValidationProblem.ts:54:3` (problem-class)

<a id="protocols-rest-validation-failed"></a>

## `protocols-rest/validation-failed`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/protocols-rest/src/libs/validators/ValidationProblem.ts:12:3` (problem-class)

<a id="public-email-domain-not-allowed"></a>

## `PUBLIC_EMAIL_DOMAIN_NOT_ALLOWED`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/invitation-core/src/libs/problems/DomainPolicyProblems.ts:5:5` (problem-constructor)

<a id="rate-limit-exceeded"></a>

## `RATE_LIMIT_EXCEEDED`

- Category: `TooManyRequests`
- HTTP status: `429` Too Many Requests
- Retryability: `retryable`
- Redaction policy: `safe-message`
- Cause: The caller exceeded a rate, quota, or concurrency limit.
- User action: Wait for the retry window or reduce request volume.
- Operator action: Check limiter state, quota configuration, and abuse signals.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/ratelimit-core/src/libs/problems/RateLimitExceededProblem.ts:15:5` (problem-constructor)

<a id="rate-limit-key-builder-error"></a>

## `RATE_LIMIT_KEY_BUILDER_ERROR`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/ratelimit-core/src/libs/problems/RateLimitConfigProblems.ts:4:3` (problem-class)

<a id="rate-limit-refund-unsupported"></a>

## `RATE_LIMIT_REFUND_UNSUPPORTED`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/ratelimit-core/src/libs/problems/RateLimitConfigProblems.ts:25:5` (problem-constructor)

<a id="rate-limit-window-error"></a>

## `RATE_LIMIT_WINDOW_ERROR`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/ratelimit-core/src/libs/problems/RateLimitConfigProblems.ts:14:3` (problem-class)

<a id="ratelimit-upstash-invalid-policy"></a>

## `ratelimit-upstash/invalid-policy`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/ratelimit-upstash/src/libs/problems/RateLimitUpstashProblems.ts:7:3` (problem-class)

<a id="ratelimit-upstash-missing-config"></a>

## `ratelimit-upstash/missing-config`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/ratelimit-upstash/src/libs/problems/RateLimitUpstashProblems.ts:21:3` (problem-class)

<a id="repository-core-batch-loader-factory-not-registered"></a>

## `repository-core/batch-loader-factory-not-registered`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/repository-core/src/libs/problems/BatchLoadProblems.ts:4:3` (problem-class)

<a id="repository-core-batch-loader-factory-resolution-failed"></a>

## `repository-core/batch-loader-factory-resolution-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/repository-core/src/libs/problems/BatchLoadProblems.ts:17:3` (problem-class)

<a id="resend-notification-failed"></a>

## `RESEND_NOTIFICATION_FAILED`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/notifications-resend/src/libs/problems/ResendNotificationProblem.ts:5:5` (problem-constructor)

<a id="retry-aborted"></a>

## `RETRY_ABORTED`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/retry-core/src/libs/errors/RetryAbortedProblem.ts:7:3` (problem-class)

<a id="retry-circuit-breaker-invalid-state"></a>

## `RETRY_CIRCUIT_BREAKER_INVALID_STATE`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/retry-core/src/libs/errors/RetryInfrastructureProblem.ts:7:3` (problem-class)

<a id="retry-circuit-breaker-lock-failed"></a>

## `RETRY_CIRCUIT_BREAKER_LOCK_FAILED`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/retry-core/src/libs/errors/RetryInfrastructureProblem.ts:20:3` (problem-class)

<a id="retry-exhausted"></a>

## `RETRY_EXHAUSTED`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/retry-core/src/libs/errors/RetryExhaustedProblem.ts:7:3` (problem-class)

<a id="retry-core-circuit-breaker-unexpected-state"></a>

## `retry-core/circuit-breaker-unexpected-state`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/retry-core/src/libs/problems/CircuitBreakerProblems.ts:7:3` (problem-class)

<a id="role-hierarchy-violation"></a>

## `ROLE_HIERARCHY_VIOLATION`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/membership-core/src/libs/problems/MembershipProblems.ts:52:5` (problem-constructor)

<a id="rpc-codegen-controller-typescript-diagnostics"></a>

## `rpc-codegen/controller-typescript-diagnostics`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/rpc-codegen/src/libs/loadRoutes.ts:40:5` (problem-constructor)

<a id="rpc-codegen-invalid-contract"></a>

## `rpc-codegen/invalid-contract`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/rpc-codegen/src/libs/generate.ts:80:5` (problem-constructor)

<a id="rpc-codegen-no-rest-controllers-found"></a>

## `rpc-codegen/no-rest-controllers-found`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/rpc-codegen/src/libs/loadRoutes.ts:19:5` (problem-constructor)

<a id="rpc-codegen-unsupported-form-schema"></a>

## `rpc-codegen/unsupported-form-schema`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/rpc-codegen/src/libs/generate.ts:86:5` (problem-constructor)

<a id="saas-demo-demo-endpoint-disabled"></a>

## `saas-demo/demo-endpoint-disabled`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/templates/saas/apps/api-server/src/problems.ts:4:3` (problem-class)

<a id="saas-demo-invalid-jobs-query"></a>

## `saas-demo/invalid-jobs-query`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/templates/saas/apps/api-server/src/problems.ts:30:3` (problem-class)

<a id="saas-demo-invalid-port"></a>

## `saas-demo/invalid-port`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/templates/saas/apps/api-server/src/problems.ts:17:3` (problem-class)

<a id="saas-demo-job-not-found"></a>

## `saas-demo/job-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/templates/saas/apps/api-server/src/problems.ts:39:3` (problem-class)

<a id="saas-demo-smoke-failed"></a>

## `saas-demo/smoke-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/templates/saas/apps/api-server/src/problems.ts:66:3` (problem-class)

<a id="saas-demo-tenant-already-exists"></a>

## `saas-demo/tenant-already-exists`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/templates/saas/apps/api-server/src/problems.ts:48:3` (problem-class)

<a id="saas-demo-tenant-not-found"></a>

## `saas-demo/tenant-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/templates/saas/apps/api-server/src/problems.ts:57:3` (problem-class)

<a id="search-capability-unavailable"></a>

## `SEARCH_CAPABILITY_UNAVAILABLE`

- Category: `NotImplemented`
- HTTP status: `501` Not Implemented
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested capability is not supported by this runtime or adapter.
- User action: Use a supported capability or choose an adapter/runtime that provides it.
- Operator action: Check runtime capability declarations and provider maturity documentation.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/search-core/src/libs/problems/SearchProblems.ts:47:5` (problem-constructor)

<a id="search-drizzle-invalid-row"></a>

## `SEARCH_DRIZZLE_INVALID_ROW`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/search-drizzle/src/libs/problems/InvalidSearchRowProblem.ts:7:3` (problem-class)

<a id="search-core-transform-not-found"></a>

## `search-core/transform-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/search-core/src/libs/problems/SearchProblems.ts:17:3` (problem-class)

<a id="search-meilisearch-tenant-token-not-configured"></a>

## `search-meilisearch/tenant-token-not-configured`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/search-meilisearch/src/libs/problems/MeilisearchProblems.ts:7:3` (problem-class)

<a id="seat-limit-exceeded"></a>

## `SEAT_LIMIT_EXCEEDED`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/membership-core/src/libs/problems/MembershipProblems.ts:78:5` (problem-constructor)

<a id="self-impersonation-not-allowed"></a>

## `SELF_IMPERSONATION_NOT_ALLOWED`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/impersonation-core/src/libs/problems/ImpersonationProblems.ts:4:3` (problem-class)

<a id="starter-invalid-environment"></a>

## `starter/invalid-environment`

- Category: `ValidationError`
- HTTP status: `422` Validation Error
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The request or generated contract failed schema or semantic validation.
- User action: Fix the invalid fields and retry with schema-conformant input.
- Operator action: Inspect schema diagnostics, generated contracts, and validation metadata.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/templates/spa-be-split/apps/api-server/src/problems.ts:4:3` (problem-class)

<a id="starter-user-not-found"></a>

## `starter/user-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/create-croco-app/templates/spa-be-split/apps/api-server/src/problems.ts:13:3` (problem-class)

<a id="storage-delete-failed"></a>

## `STORAGE_DELETE_FAILED`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/storage-core/src/libs/problems/DeleteFailedProblem.ts:8:5` (problem-constructor)

<a id="storage-file-not-found"></a>

## `STORAGE_FILE_NOT_FOUND`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/storage-core/src/libs/problems/FileNotFoundProblem.ts:11:5` (problem-constructor)

<a id="storage-invalid-key"></a>

## `STORAGE_INVALID_KEY`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/storage-core/src/libs/problems/InvalidKeyProblem.ts:5:3` (problem-class)

<a id="storage-r2-empty-body"></a>

## `STORAGE_R2_EMPTY_BODY`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/storage-r2/src/libs/problems/EmptyR2BodyProblem.ts:9:5` (problem-constructor)

<a id="storage-r2-missing-config"></a>

## `STORAGE_R2_MISSING_CONFIG`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/storage-r2/src/libs/problems/MissingR2ConfigProblem.ts:8:3` (problem-class)

<a id="storage-r2-object-too-large"></a>

## `STORAGE_R2_OBJECT_TOO_LARGE`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/storage-r2/src/libs/problems/R2ObjectTooLargeProblem.ts:8:3` (problem-class)

<a id="storage-upload-failed"></a>

## `STORAGE_UPLOAD_FAILED`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/storage-core/src/libs/problems/UploadFailedProblem.ts:11:5` (problem-constructor)

<a id="storage-cloudinary-invalid-upload-intent-ttl"></a>

## `storage-cloudinary/invalid-upload-intent-ttl`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/storage-cloudinary/src/libs/CloudinaryProvider.ts:396:13` (problem-factory)

<a id="storage-invalid-upload-intent-ttl"></a>

## `storage/invalid-upload-intent-ttl`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/storage-cloudflare/src/libs/CloudflareImagesProvider.ts:233:13` (problem-factory)

<a id="strategy-unavailable"></a>

## `STRATEGY_UNAVAILABLE`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/search-core/src/libs/problems/SearchProblems.ts:26:5` (problem-constructor)

<a id="structured-output-error"></a>

## `STRUCTURED_OUTPUT_ERROR`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-core/src/libs/problems/LlmServiceProblem.ts:52:5` (problem-constructor)

<a id="tasks-core-duplicate-task-registration"></a>

## `tasks-core/duplicate-task-registration`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tasks-core/src/libs/problems/TasksProblems.ts:13:5` (problem-constructor)

<a id="tasks-core-task-not-found"></a>

## `tasks-core/task-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tasks-core/src/libs/problems/TasksProblems.ts:4:3` (problem-class)

<a id="tasks-core-task-runner-di-failure"></a>

## `tasks-core/task-runner-di-failure`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tasks-core/src/libs/problems/TasksProblems.ts:29:5` (problem-constructor)

<a id="tasks-qstash-invalid-publish-request"></a>

## `tasks-qstash/invalid-publish-request`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tasks-qstash/src/libs/problems/QStashTaskProblems.ts:18:3` (problem-class)

<a id="tasks-qstash-missing-config"></a>

## `tasks-qstash/missing-config`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tasks-qstash/src/libs/problems/QStashTaskProblems.ts:4:3` (problem-class)

<a id="telemetry-runtime-error"></a>

## `TELEMETRY_RUNTIME_ERROR`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/telemetry-sdk-node/src/libs/problems/TelemetryProblems.ts:32:3` (problem-class)

<a id="telemetry-sampler-invalid-config"></a>

## `TELEMETRY_SAMPLER_INVALID_CONFIG`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/telemetry-sdk-node/src/libs/problems/TelemetryProblems.ts:7:3` (problem-class)

<a id="tenant-core-admin-bypass-reason-required"></a>

## `tenant-core/admin-bypass-reason-required`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tenant-core/src/libs/problems/TenantIsolationProblems.ts:60:5` (problem-constructor)

<a id="tenant-core-cross-tenant-leak"></a>

## `tenant-core/cross-tenant-leak`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tenant-core/src/libs/problems/TenantIsolationProblems.ts:79:5` (problem-constructor)

<a id="tenant-core-default-tenant-fallback"></a>

## `tenant-core/default-tenant-fallback`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tenant-core/src/libs/problems/TenantIsolationProblems.ts:48:5` (problem-constructor)

<a id="tenant-core-duplicate-tenant-manager-registration"></a>

## `tenant-core/duplicate-tenant-manager-registration`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tenant-core/src/libs/problems/DuplicateTenantManagerRegistrationProblem.ts:4:3` (problem-class)

<a id="tenant-core-isolation-context-missing"></a>

## `tenant-core/isolation-context-missing`

- Category: `Unauthorized`
- HTTP status: `401` Unauthorized
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The request did not include valid authentication credentials.
- User action: Sign in again or provide a valid credential.
- Operator action: Check authentication configuration, token issuer, and clock skew.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tenant-core/src/libs/problems/TenantIsolationProblems.ts:39:5` (problem-constructor)

<a id="tenant-core-tenant-manager-not-registered"></a>

## `tenant-core/tenant-manager-not-registered`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tenant-core/src/libs/problems/TenantManagerNotRegisteredProblem.ts:4:3` (problem-class)

<a id="tenant-core-unsafe-query"></a>

## `tenant-core/unsafe-query`

- Category: `Forbidden`
- HTTP status: `403` Forbidden
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The authenticated caller is not allowed to perform the requested action.
- User action: Request the required permission or choose an allowed action.
- Operator action: Review policy, role, tenant, entitlement, and impersonation context.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tenant-core/src/libs/problems/TenantIsolationProblems.ts:69:5` (problem-constructor)

<a id="tenant-not-found"></a>

## `tenant/not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tenant-core/src/libs/problems/TenantNotFoundProblem.ts:7:3` (problem-class)

<a id="tenant-required"></a>

## `tenant/required`

- Category: `Unauthorized`
- HTTP status: `401` Unauthorized
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The request did not include valid authentication credentials.
- User action: Sign in again or provide a valid credential.
- Operator action: Check authentication configuration, token issuer, and clock skew.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tenant-core/src/libs/problems/TenantRequiredProblem.ts:7:3` (problem-class)

<a id="testing-after-commit-hooks-failed"></a>

## `testing/after-commit-hooks-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/testing/src/libs/testing.ts:175:5` (problem-constructor)

<a id="testing-telemetry-provider-already-installed"></a>

## `testing/telemetry-provider-already-installed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/testing/src/libs/telemetry-testing.ts:54:5` (problem-constructor)

<a id="testing-transaction-context-not-active"></a>

## `testing/transaction-context-not-active`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/testing/src/libs/testing.ts:162:5` (problem-constructor)

<a id="token-limit-exceeded"></a>

## `TOKEN_LIMIT_EXCEEDED`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-core/src/libs/problems/LlmProblems.ts:21:5` (problem-constructor)

<a id="tool-execution-error"></a>

## `TOOL_EXECUTION_ERROR`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/llm-core/src/libs/problems/LlmServiceProblem.ts:61:5` (problem-constructor)

<a id="transports-graphql-request-body-aborted"></a>

## `transports-graphql/request-body-aborted`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/transports-graphql/src/libs/problems/GraphQLTransportProblems.ts:55:3` (problem-class)

<a id="transports-graphql-request-body-too-large"></a>

## `transports-graphql/request-body-too-large`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/transports-graphql/src/libs/problems/GraphQLTransportProblems.ts:33:5` (problem-constructor)

<a id="transports-graphql-resolvers-not-configured"></a>

## `transports-graphql/resolvers-not-configured`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/transports-graphql/src/libs/problems/GraphQLTransportProblems.ts:4:3` (problem-class)

<a id="transports-graphql-schema-not-configured"></a>

## `transports-graphql/schema-not-configured`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/transports-graphql/src/libs/problems/GraphQLTransportProblems.ts:16:3` (problem-class)

<a id="transports-graphql-server-not-initialized"></a>

## `transports-graphql/server-not-initialized`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/transports-graphql/src/libs/problems/GraphQLTransportProblems.ts:24:3` (problem-class)

<a id="transports-http-duplicate-health-check"></a>

## `transports-http/duplicate-health-check`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/transports-http/src/libs/HealthCheckRegistry.ts:35:13` (problem-factory)

<a id="transports-http-duplicate-route-definition"></a>

## `transports-http/duplicate-route-definition`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/transports-http/src/libs/RouteCompiler.ts:97:15` (problem-factory)

<a id="transports-http-middleware-next-called-multiple-times"></a>

## `transports-http/middleware-next-called-multiple-times`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/transports-http/src/libs/CrocoRouteRegistrar.ts:235:15` (problem-factory)

<a id="transports-http-pipe-resolution-failed"></a>

## `transports-http/pipe-resolution-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/transports-http/src/libs/ParamResolver.ts:177:13` (problem-factory)

<a id="transports-http-provider-resolution-failed"></a>

## `transports-http/provider-resolution-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/transports-http/src/libs/RouteCompiler.ts:50:11` (problem-factory)

<a id="transports-http-route-method-not-function"></a>

## `transports-http/route-method-not-function`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/transports-http/src/libs/RouteCompiler.ts:157:17` (problem-factory)

<a id="transports-http-runtime-capability-invalid"></a>

## `transports-http/runtime-capability-invalid`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/transports-http/src/libs/runtimeContext.ts:87:3` (problem-class)

<a id="transports-http-security-middleware-validation"></a>

## `transports-http/security-middleware-validation`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/transports-http/src/libs/CrocoApp.ts:169:11` (problem-factory)

<a id="transports-http-unsupported-route-method"></a>

## `transports-http/unsupported-route-method`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/transports-http/src/libs/CrocoRouteRegistrar.ts:218:15` (problem-factory)

<a id="triggers-core-duplicate-trigger-metadata"></a>

## `triggers-core/duplicate-trigger-metadata`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/triggers-core/src/libs/TriggerRegistry.ts:69:17` (problem-factory)

<a id="triggers-core-duplicate-trigger-metadata-entry"></a>

## `triggers-core/duplicate-trigger-metadata-entry`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/triggers-core/src/libs/TriggerRegistry.ts:121:17` (problem-factory)

<a id="triggers-qstash-duplicate-schedule-id"></a>

## `triggers-qstash/duplicate-schedule-id`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/triggers-qstash/src/libs/QStashScheduler.ts:247:15` (problem-factory)

<a id="triggers-qstash-execution-failed"></a>

## `triggers-qstash/execution-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/triggers-qstash/src/libs/QStashTriggerHandler.ts:248:13` (problem-metadata)

<a id="triggers-qstash-service-resolution-failed"></a>

## `triggers-qstash/service-resolution-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/triggers-qstash/src/libs/QStashTriggerHandler.ts:238:15` (problem-metadata)

<a id="tx-core-after-commit-hooks-failed"></a>

## `tx-core/after-commit-hooks-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tx-core/src/libs/problems/TransactionProblems.ts:34:3` (problem-class)

<a id="tx-core-decorator-misuse"></a>

## `tx-core/decorator-misuse`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tx-core/src/libs/problems/TransactionProblems.ts:12:3` (problem-class)

<a id="tx-core-duplicate-tx-manager-registration"></a>

## `tx-core/duplicate-tx-manager-registration`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tx-core/src/libs/errors.ts:7:3` (problem-class)

<a id="tx-core-manager-not-registered"></a>

## `tx-core/manager-not-registered`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tx-core/src/libs/errors.ts:23:3` (problem-class)

<a id="tx-core-missing-transaction-context"></a>

## `tx-core/missing-transaction-context`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tx-core/src/libs/problems/TransactionProblems.ts:23:3` (problem-class)

<a id="tx-core-propagation-error"></a>

## `tx-core/propagation-error`

- Category: `BusinessRuleViolation`
- HTTP status: `422` Business Rule Violation
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request is syntactically valid but violates a domain rule.
- User action: Change the workflow state or request values so the business rule is satisfied.
- Operator action: Review domain policy, entitlement, quota, and lifecycle rule evidence.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tx-core/src/libs/errors.ts:34:3` (problem-class)

<a id="tx-core-transaction-timeout"></a>

## `tx-core/transaction-timeout`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tx-core/src/libs/problems/TransactionProblems.ts:58:3` (problem-class)

<a id="tx-drizzle-rls-execute-unsupported"></a>

## `tx-drizzle/rls-execute-unsupported`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tx-drizzle/src/libs/problems/TxDrizzleProblems.ts:12:3` (problem-class)

<a id="tx-drizzle-savepoint-unsupported"></a>

## `tx-drizzle/savepoint-unsupported`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tx-drizzle/src/libs/problems/TxDrizzleProblems.ts:24:3` (problem-class)

<a id="tx-drizzle-tenant-context-required"></a>

## `tx-drizzle/tenant-context-required`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/tx-drizzle/src/libs/problems/TxDrizzleProblems.ts:4:3` (problem-class)

<a id="unauthorized"></a>

## `UNAUTHORIZED`

- Category: `Unauthorized`
- HTTP status: `401` Unauthorized
- Retryability: `not-retryable`
- Redaction policy: `safe-message`
- Cause: The request did not include valid authentication credentials.
- User action: Sign in again or provide a valid credential.
- Operator action: Check authentication configuration, token issuer, and clock skew.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/auth-core/src/libs/problems/AuthProblems.ts:4:3` (problem-class)

<a id="webhook-processing-failed"></a>

## `WEBHOOK_PROCESSING_FAILED`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/billing-polar/src/libs/problems/WebhookProcessingProblem.ts:4:3` (problem-class)

<a id="webhook-validation-failed"></a>

## `WEBHOOK_VALIDATION_FAILED`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/billing-polar/src/libs/problems/WebhookValidationProblem.ts:4:3` (problem-class)

<a id="webhooks-core-configuration"></a>

## `webhooks-core/configuration`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/webhooks-core/src/libs/problems/WebhookProblems.ts:38:11` (problem-metadata)

<a id="webhooks-core-dispatch-failed"></a>

## `webhooks-core/dispatch-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/webhooks-core/src/libs/problems/WebhookProblems.ts:117:11` (problem-metadata)

<a id="webhooks-core-duplicate-event"></a>

## `webhooks-core/duplicate-event`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/webhooks-core/src/libs/problems/WebhookProblems.ts:139:11` (problem-metadata)

<a id="webhooks-core-invalid-envelope"></a>

## `webhooks-core/invalid-envelope`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/webhooks-core/src/libs/problems/WebhookProblems.ts:74:11` (problem-metadata)

<a id="webhooks-core-invalid-fixture"></a>

## `webhooks-core/invalid-fixture`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/webhooks-core/src/libs/problems/WebhookProblems.ts:178:11` (problem-metadata)

<a id="webhooks-core-invalid-signature"></a>

## `webhooks-core/invalid-signature`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/webhooks-core/src/libs/problems/WebhookProblems.ts:54:11` (problem-metadata)

<a id="webhooks-core-reporter-failed"></a>

## `webhooks-core/reporter-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/webhooks-core/src/libs/problems/WebhookProblems.ts:162:11` (problem-metadata)

<a id="webhooks-core-unknown-event"></a>

## `webhooks-core/unknown-event`

- Category: `BadRequest`
- HTTP status: `400` Bad Request
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The caller sent malformed input or unsupported request options.
- User action: Correct the request input and retry after validation passes.
- Operator action: Inspect validation details and request logs; do not retry unchanged input.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/webhooks-core/src/libs/problems/WebhookProblems.ts:94:11` (problem-metadata)

<a id="workflow-core-duplicate-workflow-registration"></a>

## `workflow-core/duplicate-workflow-registration`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/workflow-core/src/libs/problems/WorkflowProblems.ts:15:5` (problem-constructor)

<a id="workflow-core-replay-unsupported"></a>

## `workflow-core/replay-unsupported`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/workflow-core/src/libs/problems/WorkflowProblems.ts:47:5` (problem-constructor)

<a id="workflow-core-saga-definition-invalid"></a>

## `workflow-core/saga-definition-invalid`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/workflow-core/src/libs/problems/WorkflowProblems.ts:62:5` (problem-constructor)

<a id="workflow-core-saga-execution-failed"></a>

## `workflow-core/saga-execution-failed`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/workflow-core/src/libs/problems/WorkflowProblems.ts:127:5` (problem-constructor)

<a id="workflow-core-saga-execution-not-found"></a>

## `workflow-core/saga-execution-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/workflow-core/src/libs/problems/WorkflowProblems.ts:77:3` (problem-class)

<a id="workflow-core-saga-replay-invalid"></a>

## `workflow-core/saga-replay-invalid`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/workflow-core/src/libs/problems/WorkflowProblems.ts:103:5` (problem-constructor)

<a id="workflow-core-saga-store-conflict"></a>

## `workflow-core/saga-store-conflict`

- Category: `Conflict`
- HTTP status: `409` Conflict
- Retryability: `conditional`
- Redaction policy: `safe-message`
- Cause: The request conflicts with current state or an idempotency constraint.
- User action: Refresh state, resolve the conflict, and retry with the updated intent.
- Operator action: Inspect concurrent writes, idempotency keys, and uniqueness constraints.
- Telemetry: `croco.problem.warning` (warning) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/workflow-core/src/libs/problems/WorkflowProblems.ts:92:5` (problem-constructor)

<a id="workflow-core-workflow-definition-invalid"></a>

## `workflow-core/workflow-definition-invalid`

- Category: `InternalServerError`
- HTTP status: `500` Internal Server Error
- Retryability: `conditional`
- Redaction policy: `operator-only`
- Cause: Croco or an upstream dependency failed after accepting the request.
- User action: Retry later only when the operation is idempotent or the caller owns retry safety.
- Operator action: Use traces, logs, and upstream diagnostics to isolate the failing boundary.
- Telemetry: `croco.problem.error` (error) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/workflow-core/src/libs/problems/WorkflowProblems.ts:31:5` (problem-constructor)

<a id="workflow-core-workflow-not-found"></a>

## `workflow-core/workflow-not-found`

- Category: `NotFound`
- HTTP status: `404` Not Found
- Retryability: `not-retryable`
- Redaction policy: `public`
- Cause: The requested resource or route-visible record does not exist.
- User action: Verify the identifier and refresh the resource list before retrying.
- Operator action: Confirm tenant scoping, data retention, and backing-store lookup behavior.
- Telemetry: `croco.problem.info` (info) with `problem.code`, `problem.category`, `problem.status`

Sources:

- `packages/workflow-core/src/libs/problems/WorkflowProblems.ts:5:3` (problem-class)
