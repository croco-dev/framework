/**
 * Warehouse integrations use explicit subpaths so applications opt into only the provider contract
 * they need. The legacy metrics integration is available from `@croco/warehouse-postgres/metrics`.
 */
export {};
