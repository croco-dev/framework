{
  "name": "{{projectName}}",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "docker:build": "docker compose build",
    "docker:run": "docker compose up -d"
  }
}
