{
  "name": "{{projectName}}",
  "private": true,
  "packageManager": "pnpm@12.7.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "docker:build": "docker compose build",
    "docker:run": "docker compose up -d"
  },
  "devDependencies": {
    "turbo": "2.10.2"
  }
}
