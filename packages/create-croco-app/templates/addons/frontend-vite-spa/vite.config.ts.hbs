import { crocoSpaViteConfig } from '@croco/frontend-vite';
import react from '@vitejs/plugin-react';
import { defineConfig } from 'vite';

export default defineConfig({
  plugins: [react(), ...crocoSpaViteConfig()],
});
