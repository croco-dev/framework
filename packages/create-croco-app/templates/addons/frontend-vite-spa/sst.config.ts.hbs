import type { sst } from 'sst';

export default {
  config(_input) {
    return {
      name: '{{projectName}}',
      region: 'ap-northeast-2',
    };
  },
  stacks(app) {
    app.stack('stacks/MyStack.ts');
  },
} satisfies sst.Config;
