import { crocoMetaVitePlugin } from '@croco/meta-vite';
import { defineConfig } from 'vite';

export default defineConfig({
  plugins: [crocoMetaVitePlugin()],
  server: {
    proxy: {
      '/api': 'http://localhost:3001',
    },
  },
});
