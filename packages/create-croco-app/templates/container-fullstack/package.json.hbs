{
  "name": "{{projectName}}",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "docker:build": "docker compose build",
    "docker:run": "docker compose up -d"
  }
}
