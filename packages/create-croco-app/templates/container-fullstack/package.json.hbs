{
  "name": "{{projectName}}",
  "private": true,
  "packageManager": "pnpm@10.15.1",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "docker:build": "docker compose build",
    "docker:run": "docker compose up -d"
  }
}
