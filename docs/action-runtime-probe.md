# Workflow action runtime probe

This temporary documentation-only file activates the pull-request docs build, artifact upload/download, and link-check jobs for issue #1317. It is removed after the GitHub-hosted runtime chain is verified.
